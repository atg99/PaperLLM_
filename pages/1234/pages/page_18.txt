GENERALIZED RANDOM FORESTS 1165
where /Psi1Hdenotes a version of /Psi1computed only using all the possible trees that
only rely on data from the half sample H⊂{1,...,n }(speciﬁcally, in terms of
Algorithm 1, we only use trees whose full I-subsample is contained in H). If we
could evaluate /hatwideHHS
n(x), results from Efron (1982) suggest that it would be a good
variance estimator for /Psi1, but doing so is effectively impossible computationally
as it would require growing very many forests.
Following Sexton and Laake (2009), however, we can efﬁciently approximate
/hatwideHHS
n(x)at almost no computational cost if we are willing to slightly modify our
subsampling scheme. To do so, let /lscript≥2 denote a little bag size and assume, for
simplicity, that Bis an integer multiple of it. Then we grow our forest as follows:
First, draw g=1,...,B//lscript random half-samples Hg⊂{1,...,n }of size ⌊n/2⌋,
and then generate the subsamples Ibused to build the forest in Algorithm 1such
thatIb⊆H⌈b//lscript⌉for each b=1,...,B . In other words, we now generate our forest
using little bags of /lscripttrees, where all the trees in a given bag only use data from the
same half-sample. Sexton and Laake (2009) discuss optimal choices of /lscriptfor mini-
mizing Monte Carlo error, and show that they depend on the ratio of the sampling
variance of a single tree to that of the full forest.
The upshot of this construction is that we can now identify /hatwideHHS
n(x)using a
simple variance decomposition. Writing /Psi1bfor a version of /Psi1computed only
using the bth tree, we can verify that /hatwideHHS
n(x)can be expressed in terms of the
“between groups” and “within group” variance terms,
Ess/bracketleftBigg/parenleftBigg
1
/lscript/lscript/summationdisplay
b=1/Psi1b−/Psi1/parenrightBigg2/bracketrightBigg
=/hatwideHHS
n(x)+1
/lscript−1Ess/bracketleftBigg
1
/lscript/lscript/summationdisplay
b=1/parenleftBigg
/Psi1b−1
/lscript/lscript/summationdisplay
b=1/Psi1b/parenrightBigg2/bracketrightBigg
,
where Essdenotes expectations over the subsampling mechanism while holding
the data ﬁxed. We deﬁne our feasible boostrap of little bags variance estimator
/hatwideHBLB
n(x)via a version of the above ANOV A decomposition that uses empirical
moments and note that, given a large enough number of trees B, this converges to
the ideal half-sampling estimator.
The result below veriﬁes that, under the conditions of Theorem 5, the opti-
mal half-sampling estimator /hatwideHHS
n(x)with plug-in values for (ˆθ(x),ˆν(x)) as in
(18) consistently estimates the sampling variance of /Psi1(θ(x),ν(x)) .W eh a v ea l -
ready seen above that the computationally feasible estimator /hatwideHBLB
n(x)will match
/hatwideHHS
n(x)whenever Bis large enough and so, given any consistent estimator /hatwideVn(x)
forV( x) , we ﬁnd that the conﬁdence intervals built using (16) will be asymptoti-
cally valid.
THEOREM 6. Given the conditions of Theorem 5,/hatwideHHS
n(x) is consistent,
/bardbl/hatwideHHS
n(x)−Hn(x;θ(x),ν(x))/bardbl F//bardblHn(x;θ(x),ν(x))/bardbl F→p0.Moreover ,given
any consistent /hatwideVn(x)estimator for V( x) such that /bardbl/hatwideV( x)−V( x)/bardblF→p0,Gaus-
sian conﬁdence intervals built using (16) will asymptotically have nominal cover-
age.