taxonomy. Furthermore, it highlights the main strengths and weaknesses of each meta-feature,
identifying important reproducibility issues related to them. Finally, the paper presents the
Meta-Feature Extractor (MFE) tool to compute many of these meta-features. Publicly avail-
able as a package in Python1and in R2, MFE oers a 
exible and standalone implementation
of meta-features for MtL experiments.
2. Taxonomy
LetDbe a dataset with ninstances, such that D=f(~ xi;yi)j1ing. Each instance
~ xi= [vi1;vi2;:::;vid] is a vector with dpredictive attribute values and a target attribute, yi. A
meta-feature fis a function f:D!Rkthat, when applied to a dataset D, returns a set of k
values that characterize the dataset, and that are predictive for the performance of algorithms
when they are applied to the dataset. Function fcan be detailed as
f(D) =(m(D; hm); hs);
such thatm:D !Rk0is a characterization measure; :Rk0!Rkis a summarization
function;hmandhsare hyperparameters used for mand, respectively. The summarization
function is required in propositional scenarios when a xed cardinality is needed, given that k
is always constant regardless of the value of k0.
Traditionally, no distinction has been made between the concepts of a meta-feature, f,
and a characterization measure, m. This may be natural when a measure results in a single
value (k0=k= 1) andis the identity function, thus f=m. However, when a measure
mcan extract more than one value from each dataset, i.e. k0can vary according to D, these
values still need to be mapped to a vector of xed length k. For instance, many authors
usefmean (m) [3, 17, 75]. Other common summarization functions are histograms [37],
minimum and maximum [79], and skewness and kurtosis [64].
These denitions allow the categorization of meta-features in a well-dened taxonomy, illus-
trated in Table 1. In this framework, categories are divided into two groups, input and output ,
which are related to the characterization of the input and output of a measure, respectively.
While some of these categories are only descriptive, others dene whether or not a meta-feature
is suitable for a specic scenario.
Some measures are restricted to specic tasks, such as classication . Others can be more
generically applied to supervised tasks, such as regression problems. The measures classied as
1https://pypi.org/project/pymfe/
2https://CRAN.R-project.org/package=mfe
3