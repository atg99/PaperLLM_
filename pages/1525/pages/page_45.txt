Published as a conference paper at ICLR 2021
Figure 12: (L1 function). Both panels show the learned v.s. true y=jxj. In the top panel, the MLP
successfully learns to extrapolate the absolute function. In the bottom panel, an MLP with different
hyper-parameters fails to extrapolate. In each ﬁgure, we color OOD predictions by MLPs in black,
underlying function in grey, and in-distribution predictions in blue.
45