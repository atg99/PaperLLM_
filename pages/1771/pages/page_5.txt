Agnostic Federated Learning
Theorem 1 and Lemma 1 also provide guidelines for choos-
ing the domains and . Whenpis large and =p, then,
the number of samples per domain could be small, the skew-
ness parameter s(/parallel.alt1m)=max 1≤k≤p1
mkwould then be
large and the generalization guarantees for the model would
become weaker. We suggest some guidelines for choosing
domains in Appendix B. We further note that, for a given
p, ifcontains distributions that are close to m, then the
model generalizes well.
The corollary above can be straightforwardly extended to
cover the case where the test samples are drawn from
some distribution D, instead of D. Deﬁne`1(D;D)
by`1(D;D)=min∈`1(D;D). Then, the following
result holds.
Corollary 1. Assume that the loss function `is bounded by
M. Then, for any >0and>0, with probability at least
1−, the following inequality holds for all h∈H:
LD(h)≤LD(h)+2Rm(G;)+M`1(D;D)+M
+M/radical.alt4
s(/parallel.alt1m)
2mlog/divides.alt0/divides.alt0
:
One straightforward choice of the parameter is=1√m,
but, depending on /divides.alt0/divides.alt0and other parameters of the bound,
more favorable choices may be possible. We conclude this
section by adding that alternative learning bounds can be
derived for this problem, as discussed in Appendix D.
4. Algorithm
4.1. Regularization
The learning guarantees of the previous section suggest
minimizing the sum of the empirical AFL term LD(h), a
term controlling the complexity of Hand a term depending
on the skewness parameter. Observe that, since LD(h)is
linear in, the following equality holds:
LD(h)=LDconv ()(h); (5)
where conv()is the convex hull of . Assume that His a
vector space that can be equipped with a norm /parallel.alt1⋅/parallel.alt1, as with
most hypothesis sets used in learning applications. Then,
given and the regularization parameters ≥0and
≥0,
our learning guarantees suggest the following minimization
problem:
min
h∈Hmax
∈conv()LD(h)+
/parallel.alt1h/parallel.alt1+2(/parallel.alt1m):(6)
This deﬁnes our algorithm for AFL.
Assume that `is a convex function of its ﬁrst argument.
Then, LD(h)is a convex function of h. Since /parallel.alt1h/parallel.alt1is
a convex function of hfor any choice of the norm, fora ﬁxed, the objective LD(h)+
/parallel.alt1h/parallel.alt1+2(/parallel.alt1m)is
a convex function of h. The maximum over (taken in
any set) of a family of convex functions is convex. Thus,
max∈conv()LD(h)+
/parallel.alt1h/parallel.alt1+2(/parallel.alt1m)is a convex
function ofhand, when the hypothesis set His a convex,
(6)is a convex optimization problem. In the next subsection,
we present an efﬁcient optimization solution for this prob-
lem in Euclidean norm, for which we prove convergence
guarantees. In Appendix F.1, we generalize the results to
other norms.
4.2. Optimization algorithm
When the loss function `is convex, the AFL minmax opti-
mization problem above can be solved using projected gradi-
ent descent or other instances of the generic mirror descent
algorithm (Nemirovski & Yudin, 1983). However, for large
datasets, that is pandmlarge, this can be computationally
costly and typically slow in practice. Juditsky, Nemirovski,
and Tauvel (2011) proposed a stochastic Mirror-Prox algo-
rithm for solving stochastic variational inequalities, which
would be applicable in our context. We present a simpliﬁed
version of their algorithm for the AFL problem that admits
a more straightforward analysis and that is also substantially
easier to implement.
Our optimization problem is over two sets of parameters,
the hypothesis h∈Hand the mixture weight ∈. In
what follows, we will denote by Wa non-empty subset of
RNandw∈Wa vector of parameters deﬁning a predictor
h. Thus, we will rewrite losses and optimization solutions
only in terms of w, instead ofh. We will use the following
notation:
L(w;)=p
/summation.disp
k=1kLk(w); (7)
where Lk(w)stands for L̂Dk(h), the empirical loss of
hypothesish∈H(corresponding to w) on domain k:
Lk(w)=1
mk∑mk
i=1`(h(xk;i);yk;i):We will consider the
unregularized version of problem (6). We note that regular-
ization with respect to wdoes not make the optimization
harder. Thus, we will study the following problem given by
the set of variables w:
min
w∈Wmax
∈L(w;): (8)
Observe that problem (8)admits a natural game-theoretic
interpretation as a two-player game, where nature selects
∈to maximize the objective, while the learner seeks
w∈Wminimizing the loss. We are interested in ﬁnding
the equilibrium of this game, which is attained for some w∗,
the minimizer of Equation 8 and ∗∈, the hardest domain
mixture weights. At the equilibrium, moving waway from
w∗orfrom∗, increases the objective function. Hence,
∗can be viewed as the center of in the manifold imposed