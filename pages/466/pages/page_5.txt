Duo-SegNet 5
Remark 1. The basis of having a critic is that a well trained critic can produce
pixel-wise uncertainty map/condence map which imposes a higher-order con-
sistency measure of prediction and ground truth. So, it infers the pixels where
the prediction masks are close enough to the ground truth distribution.
Therefore, the critic loss is dened as:
Lc(;C;D) =Ladv1(;C;X) +Ladv2(;C;U): (8)
With this aggregated adversarial loss, we co-train two segmentation networks to
fool the critic by maximizing the condence of the predicted segmentation being
generated from the ground truth distribution. To train the critic, we use labeled
prediction distribution along with given ground truth segmentation masks and
calculate the normalized loss as in Eq.(6). The overall min-max optimization
process is summarized in Algorithm 1.
Algorithm 1: Duo-SegNet ( training )
Input: Dene Segmentation networks fFi()g2
i=1, critic (), batch sizeB,
maximum epoch Emax, number of steps ksandkcfor segmentation
networks and critic, Labeled images X=f(X1;Y1);:::; (Xm;Ym)g, Unlabeled
imagesU=fU1;:::;U ngand two labeled sets X1;X2X;
Output: Network Parameters fig2
i=1andC;
Initialize Network Parameters fig2
i=1andC;
forepoch = 1,,Emaxdo
forbatch = 1,,Bdo
forkssteps do
Generate predictions for labeled data F1(x)for allXi2X1,F2(x)for
allXi2X2and for unlabeled data F1(x)andF2(x)for allUi2U;
Generate condence maps for all predictions using  ();
LetL=Ls+Lu+Lc, as dened in Equations. (2) - (8);
Updatefig2
i=1by descending its stochastic gradient on L;
end
forkcsteps do
Generate condence maps for all labeled predictions and ground
truth masks using  ();
LetLc=Ladv1, as dened in 6;
UpdateCby ascending its stochastic gradient on Lc;
end
end
end
3 Related Work
We begin by brie
y discussing alternative approaches to semi-supervised learn-
ing, with a focus on the most relevant ones to our approach, namely pseudo