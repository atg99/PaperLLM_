Appl. Sci. 2021 ,11, 8823 7 of 15
Environment
ICMEnvironment State
Action Values
Figure 1. The learning cycle ( Top) and schematic illustration of the NDSPI-DQN (bottom). ( Top) The
agent observes current environment state stand outputs an action ataccording to the learned policy;
then, the external reward re
tis obtained from the environment. Meanwhile, ICM takes (st,at,st+1)as
input and outputs the internal reward ri
t. Both ri
tand re
tare used to optimize the policy. ( Bottom )
The details of the algorithm architecture are explained in Section 3.2. The orange layers denote fully
connected layers and the red layers denote dueling network architectures with noisy linear layers.
The input to the neural network is the vector of environment states, and the output is the action value.
Environment State
Host Values Operation Values
Figure 2. Schematic illustration of the decoupling NDSPI-DQN. Two streams share the hidden
layers and each of them outputs its Qvalue using the dueling network architecture with the noisy
linear layers.
Two streams independently output the elements of actions given the input state
using p(hjs) = so f tmax h(1
shQ(s,h,#;yh))andp(ojs) = so f tmax o(1
soQ(s,o,#;yo)). The
selected host hand operation oare then combined into the attack vector to act on the