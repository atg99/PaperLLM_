the model, we might solve the optimal control problem
maximize E!t[PN
t=0R(xt;ut)]
subject to xt+1= ^'(xt;ut) +!t; ut=t(t):
In this case, we are solving the wrong problem to get our control policies t. Not only is the model
incorrect, but this formulation requires some plausible model of the noise process. But if ^ 'andf
are close, this approach might work well in practice.
3.2 Approximate Dynamic Programming
Approximate dynamic programming approaches the RL problem by directly approximating the
optimal control cost and then solving this approximation with techniques from dynamic program-
ming. The dynamic programming solution to Problem (2.3) is based on the principle of optimality :
if you've found an optimal control policy for a time horizon of length N,1;:::;N, and you want
to know the optimal strategy starting at state xat timet, then you just have to take the optimal
policy starting at time t,t;:::;N. Dynamic programming then lets us recursively nd a control
policy by starting at the nal time and recursively solving for policies at earlier times.
Dene the Q-function for (2.3) to be the mapping
Q(x;u) = max(
Eet"NX
t=0R(xt;ut)#
:xt+1=f(xt;ut;et);(x0;u0) = (x;u))
(3.2)
The Q-function determines the value of the optimal control problem that is attained when the rst
action is set to be uand the initial condition is x. Note that it then trivially follows that the optimal
value of Problem (2.3) is max uQ(x0;u), and the optimal policy is (x0) = arg max uQ(x0;u). If we
had access to the Q-function, we'd have everything we'd need to know to take the rst step in the
optimal control problem. We can use dynamic programming to compute this Q-function and the
Q-function associated with every subsequent action. That is, we dene the terminal Q-function to
be
QN(x;u) =R(x;u);
and then dene recursively
Qk(x;u) =R(x;u) +Ee
max
u0Qk+1(f(x;u;e );u0)
: (3.3)
This is the dynamic programing algorithm in a nutshell: we can recursively dene the Q-functions
by passing backward in time, and then compute the optimal controls from any starting x0by
applying the policy that maximizes the right hand side of (3.3) at each time step. (3.3) is known
as Bellman's equation. Note that for all time, the optimal policy is uk= arg max uQk(xk;u) and
depends only on the current state.
Approximate Dynamic Programming methods typically try to compute these action-value func-
tions from data. They do so by assuming that the Q-function is stationary. (i.e., Qk(x;u) =Q(x;u)
for allkand some function Q). Such stationarity indeed arises assuming the time horizon is innite.
Consider the limit:
maximize lim N!1Eet[1
NPN
t=0R(xt;ut)]
subject to xt+1=f(xt;ut;et); ut=t(t)
(x0given).(3.4)
6