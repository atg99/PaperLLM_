Algorithm 1: Memorization eects Levenshtein detector (MELD)
Data: A generative language model g, a dataset Dof question-answer pairs, a tokenizer T, a
threshold Y
Result: A metric Zthat measures the percentage of completions that have more than Y%
overlap with the ground truth
Initialize an empty list L;
foreach (q; a)2Ddo
Split qinto two halves: q1andq2;
Tokenize q1andq2using T:t1=T(q1) and t2=T(q2);
Set the model temperature to zero and pass q1as context;
Generate ktokens from gwhere k=jt2j;
Calculate the Levenshtein distance-based ratio between t2and generated text g:
`=int(round(2:0M
jqj100))
100
wherejqjis the total number of characters in both strings, and Mis the number of
matches in the two strings.;
if` > Y then
Append `to L;
Calculate the mean of L: Z = mean(L);
Return Z;
30