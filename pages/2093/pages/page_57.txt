Algorithm 4: Probabilistic Road Map (PRM)
Input : A list of possible coordinate points xi;yi,i= 1;2;;n.
Output: The closest point towards the destination.
1G(V,E) = NULL //Initialize a graph as empty
iterationlimit = n //number of nodes to make graph out of
Rad = r //radius of neighborhoods
foriteration<iteration limit) do
2Xnew =RandomPosition ()
3Xnearest =Near (G(V,E),Xnew;Rad ) //nd all nodes within a
Rad
4Xnearest =sort(Xnearest ) //sort by increasing distance
5 fornode inXnearest do
6 ifnot ConnectedComp( Xnew ,node) & not
Obstacle(Xnew ,node) then
7 G(V,E) +=fXnew ,nodeg//add edge and node to graph
Xnew.comp += node.comp//add Xnew to connected
component
8 end if
9 end for
10end for
11Return G(V,E)
forthe G(V,E) selected do
12 set the point as the destination
13 ifpath is clear then
14 execute the movement
15 else
16 move to a random point
17 end if
18end for
19return travel path
3