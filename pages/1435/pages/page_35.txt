Once its chosen which technique of language model is being employed, we also need to
choose which decoding search technique needs to be used to find the best result from the
specified number of responses.Table 5 Advantages and disadvantages of the discussed classification models
Classification
ModelsAdvantages Disadvantages
Hidden Markov
Model [ 154]HMM can model the time distribution of
a speech signal [ 79].
It is very easy to implement.
HMM can process variable-length in-
puts.
It can model both discrete and
continuous sequences.The long term dependencies are ignored as it
is assumed that the current state is only
dependent on the preceding state [ 25].
Artificial Neural
Network [ 10]ANN is very robust.
This algorithm is self-learning and or-
ganizing.
It can easily adapt to new environments.It can often over train and get stuck in local
minima problems.
Multilayer
Perceptrons
[158]They are easy to implement.
MLP doesn ’t get stuck in local minima
as it uses a random probability
distribution.They can only take fixed input lengths.
It works only for small vocabularies.
The temporal information present in the
speech signal is ignored.
Self-Organizing
Maps [ 6]No prior information is needed for
training a SOM [ 128].
Parallel computing can be used to
compute it [ 128].
It can easily adapt to new sample points.The trial and error method is used to find its
parameters [ 128].
The mapping obtained from the training
phase might be lost when applied in the
real-world [ 128].
Radial Basis
Functions [ 104]It is very simple to implement [ 140].
RBF is very robust [ 140].
It can easily discriminate between
different words of a vocabulary [ 140].It is invariant to shift in time [ 17].
Recurrent Neural
NetworkRNNs can process variable length inputs
easily.
They retain temporal information.
Different time steps can share weightsThey are computationally expensive and
require a lot of training time.
They are more suspectable to vanishing and
exploding gradients.
Convolutional
Neural NetworkIt can easily detect important features
present in a signal.
It requires less training time.It cannot capture temporal features properly.
It cannot deal with variable size input.
Fuzzy Neural
Network [ 51]It doesn ’t get stuck at local minima [ 99].
FNN requires a relatively small amount
of data for training [ 73,99].It cannot model the time distribution of
speech signals [ 99].
Support Vector
Machines [ 177]SVM is very robust [ 79].
It does not face the problems of
over-training and being stuck in local
minima [ 79].
It can take high dimensional vectors as
input [ 79].It can only take fixed length inputs [ 79].
The computational costs increase with the
number of output classes [ 79].
SVM
(One-Against--
All) [ 89]It produces a low number of binary
SVM.This technique requires relatively more
training data.
SVM
(One-Against--
One) [ 28]This technique requires less training data
as compared to the one-against-all
technique [ 152].It creates a considerable number of binary
SVM.
This approach cannot deal with the problem
of unclassified regions [ 161].Multimedia Tools and Applications (2021) 80:9411 –9457 9445