sequence of frames that can be rearranged, and the task is to learn the correct order of these frames. The original sequence serves as the anchor, while any rearranged version acts as a positive sample. The remaining sequences in the dataset are treated as negative samples. This method leverages the temporal context of data to improve feature learning.

In summary, both jigsaw puzzle solving and frame order tasks are effective pretext tasks for unsupervised learning. They focus on reconstructing or ordering based on the inherent structure of the data, allowing models to learn meaningful representations without labeled data.