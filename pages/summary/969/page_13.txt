The paper presents an overview of the connections between planning and learning algorithms in a structured manner. It identifies two primary locations where learning occurs: the dynamics model and the policy/value function. The figure outlines various interactions through labeled arrows, each representing a specific relationship. Arrow (a) indicates planning over a learned model, while (b) highlights using a policy/value network to enhance planning. Arrow (c) refers to utilizing planning outcomes as training targets for the policy/value function, and (d) signifies acting in the real world based on planning results. Arrow (e) involves acting based on a policy/value function, while (f) represents generating training targets for the policy/value from real-world data. Finally, arrow (g) focuses on creating training targets for the model based on real-world data. The paper emphasizes that most algorithms implement only a subset of these potential connections.