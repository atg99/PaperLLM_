The appendix provides proofs for two lemmas in the context of policy gradient methods within a Markov Decision Process (MDP). 

In the proof of Lemma 1, the focus is on actions in non-terminal states. The actions are defined as "up/above" (a1) and "right" (a2). The expected value functions V for different parameterizations are calculated, demonstrating that V is non-concave by showing that the weighted average of two value functions exceeds the value of their average.

Lemma 2's proof involves analyzing the difference in expected values V under two policies. It employs a telescoping argument to relate the expected rewards over trajectories initiated from a state s. The proof illustrates that the difference in expected values can be expressed in terms of the expected advantage function A under policy π0, utilizing properties of conditional expectations to derive the final equality.

Overall, these proofs establish foundational properties of policy gradients and their implications for expected rewards in reinforcement learning contexts.