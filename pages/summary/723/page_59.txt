The paper discusses the convergence of policy gradient methods to an optimal policy, specifically focusing on the softmax parameterization. The proof of Theorem 10 is structured around several supporting lemmas, with Lemma 41 demonstrating that the value function V(t)(s) is monotonically increasing across all states s. This is established by showing that the updates defined in equation (36) lead to monotonic improvements in the context of smooth objectives, particularly when appropriate step sizes are employed.

The proof relies on the performance difference lemma, which indicates that the change in the value function between iterations is non-negative. By defining a function Fs(θs) that incorporates the action probabilities and a constant term related to the advantage function A(t)(s;a), the authors derive expressions that reinforce the monotonic improvement claim. The evaluation of the derivatives and the behavior of the function under the defined updates further supports the conclusion that the policy gradient method effectively leads to improved value function estimates over time.

In summary, the paper rigorously establishes the convergence properties of policy gradient methods, emphasizing the importance of monotonic improvement in achieving optimal policies.