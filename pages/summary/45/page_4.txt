The paper discusses the computation mechanisms of Long Short-Term Memory (LSTM) networks, focusing on the equations that define the operations of various gates and memory cells. It begins with the equations for the output gate (Equation 13), input gate (Equation 14), and forget gate (Equation 15), each utilizing weight matrices and biases, processed through a sigmoid activation function to yield outputs between 0 and 1. 

The candidate memory cell, denoted as ~Ct, is computed using a tanh activation function (Equation 16), resulting in outputs between -1 and 1. The new memory content Ct is derived from the old memory content Ct-1, the forget gate, and the input gate, as shown in Equation 17, which uses element-wise multiplication to control memory preservation.

The hidden state Ht is then computed (Equation 18) by applying the output gate to the tanh of the new memory content, ensuring the output remains between -1 and 1. The paper also introduces Deep Recurrent Neural Networks (DRNNs), explaining that they can be formed by stacking standard RNN layers. The hidden states from one layer are passed to the next layer, with the first layer's computation based on input data and previous hidden states (Equation 19), while subsequent layers utilize the hidden states from the previous layer (Equation 20). Finally, the output Ot is generated from the last layer's hidden state (Equation 21). 

Visual aids in the appendices further clarify the described processes.