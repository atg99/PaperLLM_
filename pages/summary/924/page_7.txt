The presented tables summarize the performance of various methods on the Cars-196 and In-Shop datasets, indicating the effectiveness of different loss functions in metric learning. In Table 2, recall performance is measured at various k values, revealing that methods based on direction regularization, such as DR-MS, achieve the highest recall rates, significantly outperforming traditional approaches like Triplet and Proxy-NCA losses. The results highlight a consistent trend where increasing the embedding dimension enhances performance.

Table 3 shows similar trends on the In-Shop dataset, where methods like DR-MS512 demonstrate superior performance, achieving nearly a 2% improvement over the state-of-the-art MS-Loss. Qualitative analysis further supports these findings, showcasing the DR-MS method's ability to select true-positive samples effectively.

An ablation study, detailed in Table 4, validates the proposed direction regularization. It compares the performance of direction-regularized methods against their original counterparts across different loss functions on the CUB-200 dataset. Results indicate that direction regularization consistently enhances performance, regardless of the sampling strategy employed.

Table 5 explores the impact of varying the regularization parameter γ and batch sizes on recall performance. It illustrates that optimal values of γ lead to improved recall rates, emphasizing the importance of this hyperparameter in the training process. Additionally, larger batch sizes correlate with better performance, further underscoring the method's robustness. Overall, the results affirm the advantages of incorporating direction regularization into metric learning frameworks.