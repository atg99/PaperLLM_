This section of the paper discusses the membership functions and rule base of a Type-1 Fuzzy Logic System (T1-FLS) used for robotic control. It presents the input membership functions for variables such as error (ed), rate of error (˙ed), and distance to a target (df), along with the output membership functions for velocity (v) and angular velocity (w). The fuzzy labels are defined on a normalized scale, including terms like "Negative," "Zero," and "Positive," which help categorize the inputs for the fuzzy system.

The performance of the T1-FLS is guided by a rule base that incorporates human expert knowledge, allowing for the mapping of input fuzzy sets to output fuzzy sets. The firing strength of each rule is calculated using a minimum operator to determine the degree to which the rule is activated, as represented by equation (1). The rule base is detailed in Table 1, showing the relationship between input conditions and the resulting control actions.

Furthermore, the output fuzzy sets are adjusted according to the firing strength of the rules using the Mamdani implication, as noted in equation (2). This process enables the system to derive necessary control actions based on the evaluated conditions, ultimately contributing to the robot's effective response to its environment.