RCLSTM model show a more significant decrease compared to the LSTM model. This suggests that the RCLSTM model benefits more from additional training data. Furthermore, as illustrated in Fig. 4(d), the predictive performance of the RCLSTM model improves with longer input sequences, indicating its ability to capture more intricate patterns in the data.

2) Human Mobility Prediction
The evaluation of the RCLSTM model's performance in predicting human mobility is conducted through accuracy metrics. The accuracy level is calculated based on the percentage of correct location predictions. The results indicate that the RCLSTM model achieves a comparable accuracy level to traditional models while demonstrating improved efficiency due to its sparse neural connectivity.

In summary, the RCLSTM model effectively predicts both traffic and human mobility, showing acceptable predictive capabilities and reduced computational complexity. The experiments reveal that the model's performance is influenced by the number of training samples and the length of input sequences, with a notable sensitivity to these factors. The findings suggest that the RCLSTM model is a viable option for user-mobility prediction tasks.