policy. In this framework, each agent still maintains its own actor while the critic is centralized, providing a more stable learning environment. The joint action-value function considers the actions of all agents, allowing for the effective evaluation of joint policies. This approach mitigates the non-stationarity problem faced by independent agents, as the centralized critic has access to complete information about the joint state and actions of all agents. The policy update for each agent can then be expressed as:

r✓iJ(✓i)= E~⇡~✓h
r✓ilog⇡✓i(ai|hi)−
r+V~⇡~✓(x0)−V~⇡~✓(x)i

This formulation allows agents to learn more coordinated behaviors, leading to improved performance in multi-agent environments. By leveraging centralized information during training, agents can better understand the dynamics of the entire system, resulting in more robust policies that can adapt to the actions of others in real-time. Overall, the combination of independent actors and a centralized critic provides a powerful framework for addressing the challenges in multi-agent reinforcement learning scenarios.