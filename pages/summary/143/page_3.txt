The paper presents two novel algorithms based on the Guided-V Variational Autoencoder (V AE) framework: the Unsupervised Guided-V AE and the Supervised Guided-V AE. 

The core model architecture is illustrated in Figure 1, which outlines the structural differences between the two approaches. The overall objective for both models is to maximize the Evidence Lower Bound (ELBO), which serves as an estimation for the log likelihood of the data. This is expressed mathematically in Equation 1, where the ELBO comprises a reconstruction loss and a KL divergence term that quantifies the difference between the variational distribution and the prior distribution.

In the Unsupervised Guided-V AE, a deformable PCA is integrated as a secondary decoder to enhance the training of the V AE. This approach involves defining a deformation field and content representation for the input images. The deformable PCA loss is formulated in Equation 3, which includes a reconstruction term and an orthogonal loss to maintain the properties of the PCA basis. The training objective is outlined in Equation 4, which balances the ELBO with the deformable PCA loss.

The Supervised Guided-V AE, on the other hand, incorporates ground-truth labels for various attributes of the training data. It distinguishes between a scalar latent variable for each attribute and other latent variables. The training leverages an adversarial method to maximize the likelihood of the ground-truth labels given the latent variables, as detailed in Equation 6.

Overall, the proposed methods aim to enhance the performance of variational autoencoders by incorporating guidance mechanisms through PCA in an unsupervised setting and adversarial training in a supervised context, thereby improving both the reconstruction quality and attribute representation in the generated samples.