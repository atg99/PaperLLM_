The paper discusses the integration of Squeeze-and-Excitation (SE) feature maps into a neural network designed for tomato disease classification. The SE mechanism enhances the model's ability to focus on relevant features by employing a global pooling operation followed by a weighted transformation of the input feature maps. The mathematical representation of this process is provided in Equation (3), where the operations include a sigmoid activation and ReLU activation with weights from dense layers.

Table 1 presents a comprehensive overview of the tomato disease dataset, detailing the class labels, common and scientific names of diseases, the number of images per class, and the data source. A total of 19,510 images are categorized across various diseases affecting tomatoes.

Figure 2 illustrates the architecture of the proposed lightweight model, highlighting the placement of attention modules between specific residual blocks to enhance focus on high-level features. The model also incorporates convolutional and batch normalization layers within the shortcut paths.

The Self-Attention (SA) module, depicted in Figure 5, is designed to extract distinguishing features through parallel convolutional layers and ReLU activations. The output from two convolutional branches is combined to produce an attention map, which is then utilized to generate self-attention feature maps by multiplying with the transposed feature maps from a third branch. This process culminates in the addition of scaled attention maps to the input feature maps, as expressed in Equation (4).