The paper discusses a lightweight ResNet20 architecture and introduces a Dual Attention (DA) module aimed at enhancing scene segmentation. The output features map after the self-attention (SA) module is represented mathematically by the equation Out(x), which incorporates input feature maps and features generated from three parallel convolutional paths. The softmax operation is denoted as So f t, while m represents a scaling factor, and P(x), Q(x), R(x) are derived from the convolutional paths within the SA module.

The architecture details are provided in Table 2, outlining the parameters for various blocks within the network, including convolutional layers and residual blocks, along with their respective kernel sizes, strides, and output shapes.

The DA module comprises two attention networks: position attention (PA) and channel attention (CA). The PA network is akin to the SA module but varies in activation layers and attention map generation strategies. The mathematical operations for the DA module are encapsulated in Equation (5), with Equations (6) and (7) detailing the operations for PA and CA networks, respectively. The PA network combines the input feature maps with softmax-transformed feature maps, while the CA network performs operations that enhance feature representation through multiplication and addition, ultimately contributing to improved segmentation performance.