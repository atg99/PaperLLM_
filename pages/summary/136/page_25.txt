The paper presents samples from the CA V-MAE model's reconstruction capabilities when 50% of the input is masked, using data from the VGGSound dataset, which differs from the pretraining dataset. The model was pretrained on AudioSet with a 75% masking ratio and without target normalization. The results illustrate the model's effectiveness in reconstructing audio samples despite significant input masking, showcasing its potential for audio processing tasks.