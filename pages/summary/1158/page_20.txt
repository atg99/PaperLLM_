The paper discusses the behavior of a decomposition method in optimization, specifically focusing on the convergence of the sequence {αk}. At the kth iteration, if both argm(αk) and argM(αk) are subsets of I1, it is established that the working set B is a subset of I1. The method maintains feasibility, leading to an equality that relates the sums of the dual variables at iterations k and k+1.

The analysis reveals that if m(¯α) is less than M(¯α), then certain conditions regarding the optimal variables in the working set hold true. The relationship between the dual variables at different iterations indicates that the changes in the variables maintain certain properties of the optimization problem, which ultimately leads to a contradiction if it were assumed that argm(αk) and argM(αk) could be subsets of I2.

The paper concludes that the sequence {αk} cannot converge to an optimal solution ¯α if M(¯α) is greater than m(¯α), thus reinforcing that M(¯α) equals m(¯α) under the stated conditions. This result implies that there exists a threshold index ¯k beyond which certain indices do not belong to a defined set, leading to inequalities involving the dual variables that ensure the sequence converges appropriately.

The findings also highlight that the SMO-type method primarily utilizes indices in a derived set I′, suggesting that efficient caching strategies can be implemented based on the results, with recommendations for removing elements not within a specific subset to enhance performance.