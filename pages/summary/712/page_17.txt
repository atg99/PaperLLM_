This section elaborates on the proof of Theorem 3.3, which focuses on deriving the sub-gradient of the robust value function Jρ(θ). The proof begins by defining key components, including sθ, which represents the state that maximizes the value function Vπθ(s). The expression for ˆϕs(θ) is introduced, encapsulating the average of this function under distribution ρ.

The analysis shows that ˆϕs(θ) belongs to the sub-gradient of Vπθ(s), leading to the conclusion that ψρ(θ) is also part of the sub-gradient of Jρ(θ). The proof progresses through several inequalities that compare the values of two policy parameters, θ1 and θ2, illustrating the effects of policy changes on the value function. Specifically, it establishes lower bounds for the differences in value functions, which are crucial for proving the sub-gradient properties.

The final steps of the proof involve demonstrating that the differences in the value functions can be approximated using the sub-gradient ˆϕs(θ) and a direction h, reinforcing the connection between the robust value function and its sub-gradient. Overall, this section rigorously details the mathematical foundations underpinning the robust reinforcement learning framework.