The paper discusses the mathematical framework of Long Short-Term Memory (LSTM) networks, focusing on the computations involved in the various gates used to manage the cell's content. It begins with the equations for the output gate (Ot), input gate (It), and forget gate (Ft) represented in Equations 13, 14, and 15, respectively. These gates utilize weight matrices and biases, applying the sigmoid activation function to produce outputs between 0 and 1.

The candidate memory cell (~Ct) is computed using a tanh activation function, as shown in Equation 16, allowing for outputs between -1 and 1. Equation 17 integrates the old memory content (Ct-1) with the gates to determine the new memory content (Ct) through element-wise multiplication. Finally, Equation 18 describes how the hidden states (Ht) are generated, ensuring their values also fall within the range of -1 to 1.

The paper then transitions to Deep Recurrent Neural Networks (DRNNs), explaining their structure as a stacking of ordinary RNNs. Each hidden state from one layer is passed to both the next time step of the current layer and the current time step of the next layer. The computation for the hidden states in the first layer is presented in Equation 19, while Equation 20 describes the computation for subsequent layers. The overall output (Ot) is calculated in Equation 21, utilizing the hidden state from the final layer. This framework illustrates the layered approach of DRNNs, enhancing their capability to learn complex temporal patterns.