The figures referenced depict the computational processes within Long Short-Term Memory (LSTM) networks, which are a type of recurrent neural network designed to handle sequential data. 

Figure 9 illustrates how candidate memory cells are computed in LSTM, focusing on the mechanisms that allow the network to determine which information to add to the memory. 

Figure 10 continues this theme by showing the computation of memory cells, detailing how the LSTM manages long-term dependencies in the data through its unique gating mechanisms.

Figure 11 shifts the focus to the computation of the hidden state in an LSTM, highlighting how the model outputs relevant information at each time step while maintaining the context learned from previous inputs.

These figures collectively emphasize the intricate operations of LSTMs that enable them to effectively manage and utilize information over time, addressing challenges such as vanishing gradients that can occur in traditional recurrent networks.