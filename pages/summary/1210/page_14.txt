The figures S2 and S3 illustrate the performance of the domain distribution learning network G when it is trained with images affected by different types of degradation. In Figure S2, the images are corrupted with sensor noise, where Z represents the bicubic downsampled input image, ^X is the output image transformed to the natural distribution as produced by G, and GT denotes the actual degradation applied to Z. Similarly, Figure S3 presents the scenario where images are corrupted with JPEG compression, maintaining the same definitions for Z, ^X, and GT. These visual examples highlight the network's capability to learn and recover images from various corruptions.