The proof of Proposition 3.1 utilizes the policy gradient theorem and the Fisher information matrix. It begins by calculating the gradient of the log policy, represented as ∇θlogπθ(a|s). The derivation shows that this gradient can be expressed in terms of the feature mapping φθ(s,a) and its expected value under the policy πθ. The resulting expression leads to the conclusion that the gradient of the expected reward can be represented as a function of the expected Q-values and the difference between the feature mapping for the action taken and its expected value.

In the proof of Theorem 4.9, the focus is on bounding the difference in expected rewards between the optimal policy and a given policy. The proof relies on the results from Theorem 4.8 and employs the triangle inequality to upper bound the right-hand side of the inequality. This ultimately leads to an expression that combines the differences in value functions and the feature mappings, emphasizing the role of the expected values in the analysis.

Both proofs are structured around core concepts of reinforcement learning, specifically the relationship between policy gradients, feature mappings, and the expected rewards, providing a mathematical foundation for the propositions and theorems discussed.