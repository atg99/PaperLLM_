The paper discusses the derivation and bounding of specific expressions related to reinforcement learning policies. It introduces notational simplifications by defining φi(s,a) and ∆i(a|s) for easier reference. By substituting these definitions into a given equation (E.5), the authors derive a key inequality involving logarithmic differences of policy distributions and feature mappings.

The analysis uses H¨ older’s inequality to establish upper bounds for two components labeled (i) and (ii). The first component (i) is bounded by the expression involving the parameter differences and the feature mapping. The authors define δi, which plays a crucial role in determining the bounds, and show that it is constrained within a specific set with a bounded norm.

For the second component (ii), further bounding techniques are employed. The authors utilize the triangle inequality and Cauchy-Schwartz inequality to derive an upper limit on the expression involving the feature mapping and the initial parameters. The notation M0 is introduced to denote the supremum of the inner products of feature mappings with the initial parameters across the state-action space.

The paper emphasizes the relationships between the feature mappings, policy parameters, and their norms, establishing important bounds that are essential for understanding convergence and performance in reinforcement learning contexts. Overall, the findings contribute to the theoretical framework supporting the optimization of policies in reinforcement learning.