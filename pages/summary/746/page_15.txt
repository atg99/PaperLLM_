The paper discusses the properties of a function τ(ρ) defined in relation to a sequence and its behavior under certain conditions. It begins by establishing that if M(ρ) equals zρ, then S(ρ) must equal 0, leading to a contradiction. Consequently, the threshold τ⋆ is defined in terms of M(ρ) and S(ρ), leading to the conclusion that any value of ρ satisfying a specific condition will yield the same τ(ρ).

The proof of Lemma 3 is presented in two parts: the domain of τ and its monotonicity. The domain is finite for values of ρ where S(ρ) is less than or equal to 1. The largest such index is denoted as ρmax, ensuring that S is non-decreasing and remains within the bounds of 0 to 1 for 1 ≤ ρ ≤ ρmax.

For the monotonicity, an assumption is made that Mz(ρ) equals 0. A modified sequence is defined, maintaining the top entries consistent with the original sequence. The relationship between τ(ρ) and τ(ρ+1) is analyzed, demonstrating that τ(ρ+1) is at least τ(ρ), thus establishing the monotonic nature of τ.

The paper further delves into an optimization problem involving minimizing τ under specific constraints related to S. The solution involves finding a critical point where the gradient is zero, leading to a derived expression for x⋆, which satisfies the constraints of the optimization problem. The results indicate that x⋆ meets the necessary conditions, reinforcing its validity as a solution.