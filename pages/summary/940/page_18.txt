The proof discusses bounding the probability of an event Ec_t in a tree structure. It begins by establishing an upper limit on P(Ec_t) through a series of inequalities involving the estimated mean values and variances at the nodes. The key argument is that the probability can be reduced by applying specific lemmas, leading to a bound that scales with the number of nodes and the logarithm of a parameter related to the confidence level.

Next, Lemma B.2 outlines the regret bound when the complementary events fail to hold. The proof divides the time horizon into two phases, calculating the regret in each phase and demonstrating that the probability of the failure of the high-probability event diminishes over time. This results in a conclusion that the total regret is bounded by √n with high probability.

Finally, Lemma B.3 presents inequalities at time t for a selected node and its parent, involving a local smoothness function. The inequalities provide bounds on the difference between the optimal function and the function at the selected nodes, reinforcing the conditions under which the algorithm operates effectively.

Overall, the document establishes probabilistic bounds on events and regret in a tree-based algorithm, using lemmas to support the findings.