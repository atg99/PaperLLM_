The table presents the mean and standard deviation of the mean squared test error for various regression problems, highlighting the performance of different algorithms including Gradient Boosting (GB), Adaptive Gradient Boosting (AGB), Lasso, and Random Forest (RF). The results are averaged over 100 independent replications for simulated data and 20 independent permutations for real-life datasets.

For each model and algorithm, the mean squared error (m.) and standard deviation (SD) are provided across different values of the parameter ν. The optimal number of components (T⋆) is also reported for each algorithm, indicating the number of components that yielded the best performance.

Key findings include:
- In Model 1, AGB with ν = 0.001 achieved the lowest mean squared error (0.923), while RF showed a slightly higher error (1.021).
- For Model 2, AGB again demonstrated superior performance with a mean error of 0.642, significantly lower than the others.
- Model 3 results indicated that AGB with ν = 0.001 had the best performance (0.318), while the RF algorithm yielded a higher error (0.948).
- In the categorical models (Model 1(c), Model 2(c), Model 3(c)), AGB consistently showed lower mean squared errors compared to other algorithms.

The results suggest that AGB generally provides better performance across various models and configurations, with the optimal number of components varying significantly depending on the model and algorithm.