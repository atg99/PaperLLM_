In this section, the authors present a proof of Theorem 2, which involves establishing a generalization bound for ResNet through two main steps. 

The first step focuses on deriving an upper bound on the Rademacher complexity of the hypothesis space utilized by ResNet. By applying Lemma 2 to Lemma 4, the authors derive an inequality that relates the Rademacher complexity to the covering number of the hypothesis space. They find that the optimal value for a parameter in their inequality is reached uniquely, leading to a simplified version where they set the parameter to a common choice. This results in a specific upper bound expression for the Rademacher complexity.

In the second step, the authors combine their findings from the first step with results from Lemma 1 to establish an upper bound on the generalization error. This leads to a probability expression that provides a theoretical guarantee on the performance of ResNet when applied to unseen data.

The conclusion of the paper summarizes the contributions, highlighting the upper bound for the covering number of the hypothesis space induced by deep neural networks with residual connections, specifically for ResNet. The generalization bound derived can also be extended to other advanced architectures like DenseNet and ResNeXt. The authors note that while their analysis considers the complexity of the entire hypothesis space, recent experiments suggest that stochastic gradient descent (SGD) may only explore a subset of this space, indicating a potential avenue for further research by incorporating localization properties into the analysis.