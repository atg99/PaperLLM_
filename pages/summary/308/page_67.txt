The provided tables summarize the statistics of various datasets used for node-level tasks and link prediction tasks (LC). The datasets are categorized as homogeneous (Hom) or heterogeneous (Het) based on their type. 

**Table 7** presents node-level task datasets, highlighting their source, type, number of nodes, edges, classes, and features. For instance, datasets like NCF and DBLP (SNAP) are pre-trained citation datasets with millions of nodes and edges, while datasets like Cora and CiteSeer are used for downstream citation tasks, featuring a smaller number of nodes and edges. Heterogeneous datasets include Amazon and PPI-57K, which focus on social and protein interactions, respectively.

**Table 8** details the datasets used for link prediction tasks, again distinguishing between homogeneous and heterogeneous types. It includes similar datasets to those in Table 7, such as Cora, CiteSeer, and PubMed, which are prominently utilized in citation tasks, along with social datasets like BlogCatalog and Amazon. 

Both tables emphasize the diverse applications of these datasets across different domains, including academia, social networks, and biology, providing a comprehensive overview of their characteristics and research relevance. The cited related papers indicate the scholarly context for each dataset, underscoring their significance in advancing research in these fields.