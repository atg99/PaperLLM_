The table presents performance metrics for ResNet-101 and ResNet-152 architectures on the ImageNet-1K dataset, highlighting various pruning methods applied across different years. Key metrics include baseline and pruned top-1 accuracy, the decrease in accuracy, baseline and pruned FLOPs (floating point operations), the percentage reduction in FLOPs, baseline and pruned parameters, and the percentage reduction in parameters.

For ResNet-101, the pruning methods listed include Mol-19, CHEX, ABCPruner, SMCP, DMC, RSNLI, and others, showing a range of top-1 accuracy from 74.16% to 78.80%. Notably, ABCPruner and SMCP have been used in multiple instances with varying results, indicating their effectiveness in reducing parameters and FLOPs while maintaining competitive accuracy.

ResNet-152 shows similar trends with the ABCPruner and AOFP methods, where pruned top-1 accuracy varies from 76.00% to 77.47%. The data reveal that while some pruning methods lead to a decrease in accuracy, others manage to improve or maintain accuracy with considerable reductions in FLOPs and parameters.

Overall, the findings emphasize the trade-offs between model complexity and accuracy, showcasing the advancements in pruning techniques over the years.