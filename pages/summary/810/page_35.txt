This document presents a comparative analysis of various deep learning models, specifically MobileNet, DenseNet, PreResNet, and ResNeXt, evaluated on the CIFAR-100 dataset. The results are summarized in two tables that detail the performance metrics for each model, including baseline and pruned top-1 accuracy, reductions in accuracy, floating point operations per second (FLOPs), and parameter counts.

In Table 14, MobileNet-V1 and MobileNet-V2 are assessed using different pruning methods. MobileNet-V2 shows slight improvements in top-1 accuracy with the EKG method, achieving an increase of 0.22%. However, the DNCP method resulted in a decrease of 0.22% accuracy compared to the baseline.

Table 15 includes results for DenseNet-40, PreResNet-29, PreResNet-101, and ResNeXt-20/164. Notably, DenseNet-40's performance varied with different methods, with the SCP method leading to a decrease of 1.07% in accuracy. PreResNet-29 experienced a significant accuracy drop of 10.59% with the ED method. In contrast, ResNeXt-164 achieved a marginal improvement of 0.57% in accuracy.

Overall, the tables highlight the trade-offs between model pruning and accuracy, as well as varying effects of different pruning methods across the evaluated architectures.