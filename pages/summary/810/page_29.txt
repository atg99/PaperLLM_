The table presents results for various pruning methods applied to ResNet-110 and ResNet-164 architectures on the CIFAR-10 dataset, detailing their performance in terms of accuracy, floating point operations per second (FLOPs), and model parameters. 

For ResNet-110, methods like PFP, DECORE, and CHIP show a range of Top-1 accuracy changes from -0.95% to +0.94% compared to baseline performance, with corresponding reductions in FLOPs and parameters. Notably, CHIP in 2021 achieves a significant accuracy increase of 0.94% while reducing parameters by 48.30%. 

For ResNet-164, pruning methods such as C-SGD and Hinge yield slight reductions in accuracy, with C-SGD showing a minimal decrease of -0.08%. The table indicates trends in efficiency improvements through pruning, with various methods achieving different balances between accuracy retention and resource reduction.

Overall, the data reflects ongoing advancements in model pruning techniques, with varying impacts on model performance and computational efficiency.