The tables present performance metrics for various model architectures, specifically ResNet and MobileNet, on the CIFAR-10 dataset. They include details on different pruning methods applied to these models over various years, focusing on metrics such as top-1 accuracy, floating point operations per second (FLOPs), and the number of parameters.

For ResNet models, the data shows a range of top-1 accuracy results before and after pruning, highlighting both decreases and increases in accuracy depending on the method used. For instance, ResNet-18 shows a baseline accuracy of 94.46% with OICSR in 2019, which drops to 92.44% after pruning. Conversely, the same method yields an increase to 95.10% accuracy for another configuration. The FLOPs and parameters metrics demonstrate significant reductions after pruning, indicating improved efficiency.

In the case of MobileNet architectures, similar trends are observed. For example, MobileNet-V2 achieves a baseline accuracy of 94.89% with GDP-Guo in 2021, which rises to 95.15% post-pruning. The methods also show varying degrees of impact on FLOPs and parameters, with notable reductions in computational load.

Overall, the tables provide a comprehensive overview of how different pruning methods affect model performance and efficiency across various architectures, emphasizing the balance between maintaining accuracy and reducing resource consumption.