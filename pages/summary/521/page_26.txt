The paper presents a comparative analysis of various contrastive learning models applied to UEA datasets, focusing on different levels of contrast. The results, summarized in Table XIII, indicate that models emphasizing temporal contrast, such as TS2Vec, TS-TCC, and T-Loss, outperform those centered on instance-level or prototype-level contrast. In contrast, prototype-level methods like SwA V and PCL, which excel in image data, struggle with time series data due to their reliance on flat clustering algorithms that capture only a single semantic hierarchy.

The paper further evaluates the performance of different backbone architectures in the SimCLR model for unsupervised time series representation learning, as shown in Table XIV. ResNet-18, ResNet-50, and InceptionTime are compared, with InceptionTime demonstrating superior performance. This is attributed to its design, which employs 1D convolutions with parallel kernel sizes, enabling effective feature extraction across diverse temporal scales.

Additionally, the paper analyzes various data augmentation strategies in Table XV, comparing original and specified augmentation methods with two automatic selection strategies, RandAugment and W-Augment. The specified strategy, based on empirical results and expert knowledge, aligns with the original approach in the TS-TCC model, highlighting the importance of augmentation in enhancing model performance.

Overall, the findings underscore the significance of model architecture, contrast levels, and data augmentation in achieving effective performance in time series analysis.