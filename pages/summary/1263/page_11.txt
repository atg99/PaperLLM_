standard models. The best overall performance is achieved through a voting strategy that incorporates all bagged neural network models, highlighting the effectiveness of ensemble methods in addressing the challenges posed by imbalanced datasets.

The metrics evaluated include accuracy, balanced accuracy, precision, recall, and F1 score. The results indicate that the Bagged CNN model achieves the highest accuracy at 0.8910, while the voting approach across all neural networks provides the best balanced accuracy at 0.9208. The findings suggest that ensemble learning techniques, particularly stacking models, significantly enhance predictive performance across various metrics compared to individual standard models.

In conclusion, the experiments confirm that utilizing stacking techniques, especially with bagged neural networks, yields superior results, emphasizing the importance of ensemble methods in improving classification tasks in imbalanced datasets.