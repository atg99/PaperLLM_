The table presents the mean and standard deviation of the regression root mean square error (RMSE) for various datasets when employing Extreme Learning Machine (ELM) or Support Vector Regression (SVR) as replacements for Ridge Regression (RR) in BoostTree models. The best performances are highlighted in bold, while an asterisk indicates a statistically significant advantage for BoostForest models over BoostTree.

For ELM, the average RMSE across datasets is 0.574 for ELM, 0.583 for BoostTree-ELM, and 0.499 for BoostForest-ELM. The average ranks suggest that BoostForest-ELM outperforms the other methods.

Similarly, for SVR, the average RMSE is 0.616 for SVR, 0.581 for BoostTree-SVR, and 0.509 for BoostForest-SVR, indicating that BoostForest-SVR also achieves superior performance based on average rank.

Overall, the results indicate that both BoostForest-ELM and BoostForest-SVR consistently yield lower RMSE values compared to their respective BoostTree counterparts across different datasets, demonstrating their effectiveness in regression tasks.