The paper presents training hyperparameters and tunable parameters for various parameter-efficient tuning methods applied to tasks such as summarization and machine translation. Key hyperparameters are detailed in Table 8, which includes learning rate, batch size, label smoothing, maximum gradient norm, weight decay, and training steps for tasks like XSum and MNLI/SST2.

The computation of tunable parameters is elaborated through the structure of pre-trained models like BART/mBART and RoBERTa. The number of attention and feed-forward network (FFN) sub-layers is specified, with the encoder-decoder models containing three attention sub-layers compared to RoBERTa's single attention sub-layer. The methodology for calculating tunable parameters incorporates the number of parameters in the attention and FFN layers, with specific formulas provided for each tuning method including Preﬁx Tuning, Adapter variants, and LoRA.

Table 11 summarizes the number of tunable parameters for various methods using BART/MBART models as examples, demonstrating the differences in parameter requirements across the different tuning approaches. The proposed MAM Adapter method is also included, highlighting its unique parameter configuration.

Overall, the paper emphasizes the importance of understanding the parameter efficiency of different tuning methods to optimize performance across various natural language processing tasks.