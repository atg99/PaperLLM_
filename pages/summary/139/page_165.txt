The paper discusses the marginalization of the hidden states h1:T in a probabilistic model, specifically in the context of a recurrent neural network. The key equation presented is the marginalization of the joint distribution pθ(x1:T, z1:T) with respect to the hidden states, leading to the expression in equation (A.4). This is achieved by sequentially marginalizing over the hidden states starting from hT, substituting each hT with a deterministic function fh(xT-1, zT, hT-1) at each step.

The notation is simplified by identifying the deterministic function ht with dt, emphasizing that ht is not a free random variable but rather a function dependent on the previous observations and inputs. The resulting conditional distribution pθ(x1:T|z1:T) is derived, highlighting that it can be expressed as a product over the conditional probabilities pθ(xt|ht).

The paper underscores the importance of interpreting h1:T as a set of vectors rather than random variables, reinforcing the deterministic nature of the function linking the hidden states to the observed data. This formulation is crucial for understanding the dynamics of the model and its probabilistic inference.