This section discusses the generative model's structure, highlighting the relationships between various conditional distributions and their parameters. The generative process is described through equations that illustrate how the hidden states and observations are updated over time using nonlinear mappings parameterized by deep neural networks (DNNs). Specifically, the equations for the hidden state ht and the distributions for zt and xt are presented, showing how they depend on previous observations and states.

The parameters θz and θx are defined for the conditional distributions of zt and xt, respectively, indicating whether they share parameters or not. The paper emphasizes that while the conditional distributions of zt are the same across models, the computations for their parameters differ, necessitating a distinction between different model forms. The authors propose terminology to categorize the DVAE model based on whether only random variables are present, referring to this as a compact representation. The choice and design of DNN architectures are acknowledged as application-dependent but are not the focus of this paper. Overall, the section provides a detailed mathematical framework for understanding the generative process within the model.