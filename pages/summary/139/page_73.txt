state ht-1. The formulation of Stochastic Recurrent Networks (STORN) extends the capabilities of traditional single-layer Recurrent Neural Networks (RNNs) to deeper architectures. By introducing an additional input zt, which is modeled as an independent and identically distributed (i.i.d.) standard Gaussian variable, STORN leverages the temporal dynamics of ht to effectively generate the output xt.

The equations presented define the relationships within the network: ht is derived from the previous input xt-1, the current latent variable zt, and the previous hidden state ht-1. The transformation functions dh and dx parameterize the network, with θh and θhx representing their respective parameters, while θx encompasses both sets. The output xt is generated from ht, which is influenced by the preceding states and the latent variable, thus positioning STORN within the framework of Deep Variational Autoencoders (DVAE).

The notation is aligned for consistency across models, with the modification allowing for easier comparison with prior works. This redefinition of relationships emphasizes the complexity of the states and suggests that ht, while derived deterministically, behaves as a random variable due to its dependence on zt and xt-1. Overall, STORN's architecture enables a nuanced approach to modeling sequential data, integrating stochastic elements into the recurrent structure.