The paper discusses the calculation of total partial derivatives for an objective function in the context of recurrent neural networks (RNNs). It presents the expressions for the derivatives of the objective function with respect to both the readout signal and the state signal at index n, as shown in Equations 82 and 84. These derivatives form sequences that run in the backward direction of the index n, contrasting with the forward progression of the sequence generated by Equation 74.

To initialize the sequence of total partial derivatives related to the state signal, the paper specifies that the gradient must start from a zero vector at the last step of the segment, as indicated in Equation 85. The subsequent equations detail how the derivatives of the model's parameters (Θ) are computed, emphasizing that these derivatives incorporate contributions from all unrolled steps of the RNN, as captured in Equation 90.

A critical finding is that all necessary quantities for updating the parameters during training are directly proportional to the error gradient signal, denoted as ψ[n]. When employing backpropagation through time (BPTT), this gradient flows backward through the sequence, indicating that the gradient at a given step can influence earlier steps.

The paper further explores how the gradient at a later index can affect the entire sequence and discusses the importance of retaining certain components of the gradient from previous steps to adjust the model's parameters effectively. This retention allows the model to learn from information at one sample to minimize errors at distant samples, which is crucial for handling long-range dependencies in RNNs.

The final expansion of the recursion in Equation 84 illustrates how the gradient of ψ[n] relates to the gradients at earlier steps, emphasizing the interconnectedness of the gradients across the unrolled sequence. Overall, the findings underscore the significance of backpropagating gradients effectively to enhance RNN training and optimize model parameters.