The remarks address limitations and considerations in the context of recurrent neural networks (RNNs) and their approximation techniques. 

Remark 1 highlights that Proposition 1 and its proof do not encompass advanced RNN architectures such as Gated Recurrent Units (GRUs) and Attention Networks, which may have different behaviors compared to standard RNNs.

Remark 2 emphasizes the independence of segment-level state signal subsequences while noting that input signal subsequences from different segments do not necessarily have to be independent. It suggests that dependencies in input signals can be managed through appropriately trained hidden layer weights, thereby maintaining the independence of state signal subsequences.

Remark 3 discusses the challenges of IIR-to-FIR conversion, asserting that no method is universally optimal. It argues that finding an optimal FIR approximation is contingent on specific performance measures, and that practical approximations often require balancing fidelity with adaptability to data distributions. The unrolling technique, while standard for approximating RNNs with FIR sequences, introduces artificial discontinuities due to truncation. However, if the sequence can be treated as independent segments, the unrolled RNN can effectively reproduce outputs based on inputs, akin to the Sampling Theorem in signal processing. 

Remark 4 elaborates on the applicability of Proposition 1, stating that while unrolling is justified under certain conditions, real-world data often exhibits inter-dependencies among output subsequences. It suggests that a suitable subdivision of output sequences into independent subsequences can be empirically determined through experimentation and statistical analysis, aligning better with the generative process of the input sequence.

Overall, the remarks collectively underscore the complexities and practical considerations involved in applying RNNs and their unrolling techniques in various contexts.