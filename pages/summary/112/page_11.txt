The paper compares Long Short-Term Memory (LSTM) networks and Gated Recurrent Units (GRU) in the context of learning symbolic sequences. It presents findings indicating that GRU networks require approximately half the training time of LSTM networks when fitting low complexity seed strings. Additionally, while both LSTM and GRU achieve similar forecasting performance for these sequences, LSTM performs slightly better but at the cost of increased training time.