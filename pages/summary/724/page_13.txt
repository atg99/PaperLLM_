In this section, the paper discusses the properties of p-normalized-balanced vectors, specifically focusing on the definitions of p-variance and p-mean. The p-variance, denoted as κp(v), is defined as the minimum distance of a vector v from a scaled constant vector, while the p-mean, ωp(v), represents the constant that achieves this minimum. 

A new function, referred to as the p-balanced-normalized function, is introduced to characterize the deviation of the vector elements from the p-mean, scaled by the p-variance. The paper cites a lemma from a previous work, stating that the p-variance can be viewed as the optimal value of a linear optimization problem constrained by kernel noise.

The authors then present Lemma A.1, which asserts that a specific vector, defined as c* = -ϵuq(v), is an optimal solution to the aforementioned optimization problem. The proof of this lemma involves verifying that c* meets the necessary constraints of the optimization problem and achieves the optimal value of the p-variance. 

The calculations confirm that the norm of c* satisfies the constraint, and the sum of its components yields zero, thus fulfilling the noise constraint. The authors highlight a proportional relationship involving the derivative of a function related to the p-variance, reinforcing the optimality of the proposed solution. Overall, the section provides a foundational understanding of the mathematical properties governing p-normalized-balanced vectors and their optimization characteristics.