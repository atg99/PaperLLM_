This paper discusses the optimization of transfer learning architectures through layer-wise fine-tuning and evaluation of model performance on image data. The authors present a detailed layer configuration in Table 3, which outlines parameters and layers for various architectures. They illustrate the tuning process in Figure 2, demonstrating how the ratio of trainable layers affects model accuracy. The optimal configuration is defined as the one yielding the highest accuracy.

In terms of loss functions, the study employs Categorical Cross-Entropy, suitable for multi-class classification, as expressed in Equation 1. Evaluation metrics include Recall, Accuracy, Precision, and F1 score, with specific ratios calculated for tuned layers and parameters.

The results section provides a summary of baseline transfer learning model performance without fine-tuning, detailed in Table 4. Various models, such as ResNet50, VGG16, and others, are evaluated based on their trainable parameters and accuracy. The findings indicate a correlation between the number of trainable parameters and the models' accuracy, providing insights into the effectiveness of different architectures in transfer learning tasks.