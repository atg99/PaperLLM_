The paper discusses the representation of probability distributions by Restricted Boltzmann Machines (RBMs) through the marginal distribution P(v) as expressed in equations derived from the model. Specifically, it highlights how the choice of potential U affects the representational power of the RBM, which determines the subset of distributions the RBM can capture.

Equation (2) presents P(v) as a function of a partition function Z and an exponential term involving the potential U. The authors further express P(v) in terms of a model of interacting binary variables in equation (3), where I(s) denotes the interaction terms between visible nodes. This formulation allows for a clearer understanding of how RBMs approximate complex probability distributions through interactions among binary variables.

The paper also delves into a special case involving a quadratic potential, reformulating P(v) in equation (4) and defining Z0 and the probability density ϕ(z) in equation (5). The discussion includes the cumulant generating function K, which is crucial for understanding the probabilistic behavior of the distribution defined by ϕ(z).

Overall, the paper aims to elucidate how RBMs leverage their architecture to effectively model and approximate a wide range of distributions over binary variables.