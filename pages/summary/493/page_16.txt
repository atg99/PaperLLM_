In this section, we present the numerical results that validate our theoretical discussions. We begin by comparing interaction models derived from our theoretical expressions with those obtained from simulations of simple Restricted Boltzmann Machines (RBMs). We also evaluate the performance of RBMs trained with varying numbers of hidden nodes and activation functions using the MNIST dataset.

In the first experiment, we analyze a simple RBM with three visible nodes and four hidden nodes, utilizing an Exponential activation function. By applying the mapping outlined in our previous sections, we establish that this RBM corresponds to a three-body interaction model, where the visible nodes interact solely through this term. We verify this equivalence by conducting blocked Gibbs sampling to generate configurations from the RBM and measuring the frequency of each state. The empirical frequencies align closely with the probabilities predicted by the interaction model, as illustrated in the histogram.

In the second experiment, we extend our analysis to larger RBMs consisting of ten visible nodes and fifteen hidden nodes. The weights and biases for these RBMs are randomly selected from Gaussian distributions, ensuring a diverse distribution in the state space. This setup allows us to further confirm the analytical mapping discussed earlier.

Overall, these numerical results support the theoretical framework we have established, demonstrating the validity of our interaction model and its application to RBMs.