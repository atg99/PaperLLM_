The paper discusses the performance of the SwA V method in training a ResNet-50 model under various conditions, particularly in a small batch setting and its effectiveness in transferring unsupervised features to downstream tasks. 

In Table 3, the top-1 accuracy on ImageNet using a linear classifier with frozen features shows that SwA V achieves a high accuracy of 74.3% after 400 epochs, outperforming other methods like MoCov2 and SimCLR, which require more epochs for slightly lower accuracies. SwA V utilizes a smaller feature queue of 3,840, compared to MoCov2's 65,536, and does not require a momentum encoder, making it more efficient. 

Section 4.2 highlights the generalization of SwA V features to various downstream tasks, where it surpasses supervised ImageNet features on datasets like Places205, VOC07, and iNaturalist2018. SwA V also excels in object detection tasks using Faster R-CNN and DETR, demonstrating that self-supervised methods can outperform traditional supervised pretraining in this context.

The paper's findings suggest that SwA V not only maintains state-of-the-art performance in small batch training but also establishes a new benchmark for self-supervised learning methods in feature transfer tasks. The authors have made their model available for further research, indicating a significant contribution to the field of self-supervised learning. Future work may explore combining SwA V with momentum mechanisms for potentially enhanced performance.