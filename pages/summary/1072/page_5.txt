In this section of the paper, the author discusses the role of convolutions in matrix factorizations and the construction of deep convolutional neural networks (DCNNs). The analysis begins with the product of Toeplitz-type matrices in the context of affine mappings, demonstrating how convolutions of filter masks can be expressed as a sequence of convolutions. The author introduces Lemma 1, which establishes that the Toeplitz matrix associated with a convoluted sequence is equal to the product of individual Toeplitz matrices.

Following this, Lemma 2 presents a method for factoring a sequence supported in non-negative integers into a series of convolutions of filter masks, highlighting that it is possible to represent the sequence using a finite set of masks.

The author then addresses the selection of bias vectors necessary for the explicit formulation of functions in the network. The properties of convolutional matrices are discussed, emphasizing the importance of ensuring non-negative entries in intermediate computations. The construction of the downsampling operator is also outlined, providing a matrix representation that facilitates the downsampling process in the network.

Subsequently, Lemma 3 is introduced to establish conditions for the bias vectors that maintain the integrity of the network's computations. The author demonstrates how to construct the DCNN layers and prove that ridge functions can be approximated by the network, utilizing the convolutional factorizations established earlier.

The proof of Theorem 1 illustrates the construction process of the first layers of the network, using specific sequences of filter masks and ensuring that the resulting matrix representations align with the desired function approximations. This section emphasizes the interplay between convolutions, matrix factorizations, and the design of neural network architectures, laying the groundwork for further theoretical developments and applications in DCNNs.