The appendix of the paper presents detailed algorithms for the TT-Rec model, specifically focusing on the forward propagation of TT-Embedding and the initialization of TT-cores. 

Algorithm 1 outlines the forward propagation process, which involves iterating through indices and computing values based on input embeddings. The algorithm utilizes a loop to handle the indices and employs batched General Matrix Multiply (GEMM) operations to perform matrix multiplications efficiently. The output is derived from reducing the embedding rows based on calculated indices.

This section provides a clear procedural breakdown for implementing the TT-Rec embedding tables, emphasizing computational efficiency through the use of batch processing and matrix operations in deep learning recommendation systems. The initialization method for TT-cores, mentioned in Algorithm 3, employs a Sampled Gaussian approach, which is essential for setting up the model effectively.

Overall, the algorithms serve as a technical foundation for understanding the operational mechanics of TT-Rec in the context of deep learning recommendations.