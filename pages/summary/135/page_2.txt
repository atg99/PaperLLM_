features in the context of evolving data streams. 2) It introduces an incremental learning mechanism that operates without reliance on pre-defined hyper-parameters, enhancing adaptability. 3) DEVDAN integrates both generative and discriminative processes, allowing for an evolving model that can adjust in real-time to changes in data distribution. 4) The proposed method demonstrates improved predictive performance in scenarios where true class labels are absent, effectively utilizing unlabeled data to refine the learning process. Overall, DEVDAN represents a significant advancement in the development of deep neural networks for real-time applications in dynamic environments.