This section presents the derivation of the partial derivatives of the objective function E with respect to various parameters of the Augmented LSTM cell, as defined in previous equations. The derivatives are expressed for different weights and biases across multiple components of the LSTM cell, including input, forget, output gates, and candidate layers. Each derivative is represented in terms of the activation functions and inputs at specific time steps, indexed by n and l.

The total derivative of E with respect to the model parameters Θ at step n is structured to align with the defined parameters, consolidating the individual derivatives into a comprehensive expression. This arrangement emphasizes the relationship between the model parameters and the objective function, facilitating the optimization process during training. The notation clearly indicates the dependencies of each derivative on both the current and previous time steps, highlighting the temporal nature of the LSTM architecture.