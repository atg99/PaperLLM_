The figures illustrate key components of Long Short-Term Memory (LSTM) networks. Figure 8 shows the unrolling of an LSTM cell, detailing a sequence of four steps to illustrate the temporal dynamics of the network. Figure 9 presents a vanilla LSTM cell, highlighting various stages of the system with dashed rectangles and annotations that clarify the function of each stage. Notably, the bias parameters are omitted for clarity but can be integrated into the input signal vector by adding a constant element. This approach maintains the integrity of the weight matrices while simplifying the representation.