The paper discusses the efficacy of a proposed approach through various performance metrics illustrated in Figure 3. It includes a comparison of hidden neurons used for training against conventional algorithms, root mean square error (RMSE) values during training, predicted outcomes of the HPSO-SSM-RBFNN during testing, and prediction errors observed in the testing phase.

Section 4.4 focuses on non-linear model identification, emphasizing the importance of non-linear models for accurately representing neural network performance. The performance is mathematically expressed by a specific equation (Equation 19) that incorporates two inputs, u(t) and y(t), to predict y(t+1). The training inputs are categorized for half the training duration within the range of -2 to 2, while the other half consists of a sinusoidal signal. A total of 1000 samples are designated for both training and testing processes.