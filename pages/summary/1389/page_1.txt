weights in the residual blocks, which reduces the number of parameters while maintaining performance.
3. ShelfNet achieves significantly faster inference speeds and high accuracy compared to both non real-time models like PSPNet and real-time models like BiSeNet. It demonstrates a 4x speed improvement over PSPNet while maintaining similar accuracy on the PASCAL VOC dataset and higher accuracy than BiSeNet at comparable speeds on the Cityscapes dataset. 
4. Extensive experiments validate the effectiveness of ShelfNet, achieving a mean Intersection over Union (mIoU) of 79.0% on the Cityscapes dataset with a ResNet34 backbone, outperforming larger backbone models such as ResNet101 used in PSPNet and BiSeNet.

In conclusion, ShelfNet presents a promising solution for fast semantic segmentation without compromising accuracy, making it suitable for applications where speed is crucial, such as autonomous driving. The implementation is available through the provided GitHub link.