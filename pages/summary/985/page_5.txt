The paper discusses the training and fine-tuning of various Chinese pre-trained language models, including BERT, RoBERTa, MacBERT, and ELECTRA, across different natural language processing tasks. Key datasets used for fine-tuning include Machine Reading Comprehension datasets (CMRC 2018, DRCD, CJRC) and classification datasets (ChnSentiCorp, THUCNews, XNLI, LCQMC, BQ Corpus).

Table 2 outlines the statistics and hyper-parameter settings for these tasks, indicating details such as maximum sequence length, batch size, number of epochs, and initial learning rates. The training details for the models are summarized in Table 3, highlighting the number of training steps, initial checkpoints, and optimizers used.

The models are pre-trained with a maximum sequence length of 512 for tasks requiring long sequences. The optimization techniques vary, with AdamW used for BERT and LAMB for larger batch sizes. The fine-tuning process involved consistent hyper-parameter settings across datasets, with adjustments to the initial learning rates based on performance on development sets.

The study emphasizes the importance of selecting appropriate initial learning rates and maintaining uniform settings across tasks to ensure fair comparisons. It also notes that while the initial learning rates were derived from the original Chinese BERT, further tuning could yield improved results.