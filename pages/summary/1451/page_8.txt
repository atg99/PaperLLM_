The results of various models on question generation and response generation tasks are presented in the paper. In Table 8, the performance metrics for question generation on the SQuAD dataset indicate that the UNILM model consistently outperforms others, achieving state-of-the-art scores in BLEU-4, METEOR, and ROUGE-L. The question generation task is framed as a sequence-to-sequence problem, where the model is fine-tuned on a training set, using specific hyperparameters such as batch size and learning rate.

Table 9 demonstrates that questions generated by the UNILM model significantly enhance the performance of a question-answering model, yielding improvements in F1 and EM scores. The model generates a substantial number of examples, which are subsequently used to fine-tune the question-answering system, resulting in notable performance gains.

In response generation, shown in Table 10, UNILM also performs well, achieving competitive metrics compared to human performance and other systems in the DSTC7 Shared Task. The evaluation includes metrics such as NIST-4, BLEU-4, METEOR, and diversity measures.

The paper emphasizes the effectiveness of the UNILM model in both generating questions that improve QA systems and in producing high-quality responses in dialogue contexts. The use of auxiliary tasks during fine-tuning is noted as a crucial factor in enhancing model performance and mitigating issues like catastrophic forgetting.