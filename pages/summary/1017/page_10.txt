2,000 audio clips representing different acoustic scenes. The evaluation of the models was based on accuracy, with various training strategies applied. The results summarized in Table XIV indicate that the fine-tuned system achieved an accuracy of 0.764, while the systems using PANNs as feature extractors (Freeze_L1 and Freeze_L3) performed less effectively with accuracies of 0.589 and 0.607, respectively. Training from scratch yielded an accuracy of 0.691.

3) DCASE 2018 Task 2: This task involved sound event detection, with a focus on achieving high mean average precision (mAP). The results in Table XV show that the fine-tuned system attained a high mAP@3 of 0.902, significantly outperforming the other methods, including those using PANNs as feature extractors. The Freeze_L1 and Freeze_L3 systems had mAP@3 scores of 0.717 and 0.768, respectively, while the system trained from scratch achieved an mAP@3 of 0.954.

Overall, the findings highlight the effectiveness of fine-tuning PANNs for audio pattern recognition tasks, particularly in scenarios with limited training data. The results demonstrate that using PANNs as feature extractors can be advantageous when the number of training clips is low, while fine-tuning yields superior performance with larger datasets.