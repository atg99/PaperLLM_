The paper discusses the Q-function as a fixed point of a contraction mapping, specifically defining the mapping H from B(S × A) to B(S × A). The mapping is expressed as HK(s, a) = E[w∼Pw(·|s,a)[r(s, a, w) + γmaxa′∈A(f(s, a, w), a′)] for all (s, a) in S × A. The recurrence relation for computing the QN-functions is reformulated as QN = HQN−1 for N > 1, with the initial condition Q0(s, a) set to 0.

The paper proves that H is a contraction mapping, leading to the conclusion that the sequence of QN-functions converges to the unique solution of the Bellman equation Q = HQ. Additionally, it establishes the convergence of the Q-learning algorithm using the properties of the algorithmic model. This establishes a foundation for understanding the behavior of Q-learning in relation to fixed point theory and contraction mappings.