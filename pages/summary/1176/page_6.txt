In this section of the paper, Shi et al. present a method for calculating the optimal number of clusters (Kopt) using a specific algorithm detailed in Algorithm 2. The process begins with calculating the Euclidean distance between two adjacent points, utilizing formula (4). The algorithm involves several key steps:

1. **Data Preparation**: The data points are organized into pairs of two-dimensional coordinates by zipping N(md) with the range [Kmin, Kmax], resulting in a list of point pairs, PL.

2. **Point Selection**: Three adjacent points are identified within the list, designated as Pi, Pj, and Pk.

3. **Distance Calculation**: The Euclidean distances between these points are computed, denoted as a, b, and c.

4. **Angle Calculation**: The angles formed by the three points are calculated using formula (5).

5. **Optimal Cluster Identification**: The algorithm identifies the minimal angle (αmin) and determines the index corresponding to the optimal number of clusters, Kopt.

Overall, the algorithm provides a systematic approach to determining clustering parameters through geometric calculations.