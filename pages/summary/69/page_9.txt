The section discusses the concepts of "unfolding" or "unrolling" in recurrent neural networks (RNNs), particularly in reference to Equations 30 and 32, which define the state of the RNN. Initially, the state signal is uninitialized, and the unfolding process begins by specifying initial conditions for the state signal, denoted as ⃗ s[n]. This process allows the computation of the sequence over a finite range of discrete steps indexed by n, as illustrated in a provided figure.

The recursive nature of the equations means that the state signal at any index n includes contributions from all previous states and inputs, making the RNN an example of an Infinite Impulse Response (IIR) system. The section also defines a vector-valued unit step function ⃗ u[n] and a vector-valued unit sample function ⃗δ[n], with visual representations included in the accompanying figure. These definitions help in understanding the behavior of the RNN over time, emphasizing the accumulation of inputs and states.