The document provides definitions and notation related to probability theory, specifically focusing on different probability functions and distributions. 

Key concepts include:

1. **Probability Density Function (PDF)**: A function that assigns a probability density to each possible value of a continuous random variable.
2. **Cumulative Distribution Function (CDF)**: A function that assigns a cumulative probability density to each possible value of a univariate continuous random variable.
3. **Probability Mass Function (PMF)**: A function that assigns a probability mass to each possible value of a discrete random variable.

The document also discusses various distributions, using overloaded notation to represent both the distribution itself and its corresponding PDF. Notable distributions mentioned include:

- **Categorical Distribution**: Defined by a parameter p where the sum of probabilities equals one.
- **Bernoulli Distribution**: Multivariate distribution of independent Bernoulli trials.
- **Multivariate Normal Distribution**: Represented as N(x; µ, Σ), characterized by a mean vector µ and a covariance matrix Σ.

Additionally, the chain rule of probability is introduced, expressing the joint probability of two events a and b in terms of their conditional probabilities. 

This summary encapsulates the core definitions and concepts without omitting essential information.