The paper presents a comprehensive evaluation of various models for hierarchical human parsing, focusing on the performance of HSSN (Hierarchical Semantic Segmentation Network) across different datasets, including PASCAL-Person-Part and LIP. The results demonstrate that HSSN consistently outperforms other state-of-the-art models, such as DeepLabV3+ and MaskFormer, across multiple metrics (mIoU1, mIoU2, mIoU3) and various backbone architectures (ResNet-101, HRNet-W48, Swin-Small).

Table 3 illustrates the performance of several models on the PASCAL-Person-Part test dataset, where HSSN achieves superior results in parsing different human body parts. Similarly, Table 4 shows HSSN's competitive performance on the LIP validation dataset, outperforming the leading human parsers by notable margins. The paper emphasizes the significance of structured label constraints and representation learning in enhancing segmentation accuracy.

Qualitative results further highlight HSSN's robustness in challenging scenarios, showcasing its ability to accurately distinguish between different concepts and avoid misclassification errors that other models, like MaskFormer, encounter. The diagnostic experiments reveal that key components of HSSN, such as hierarchical segmentation learning and representation learning, contribute significantly to its performance improvements over baseline models.

Overall, HSSN establishes new benchmarks for hierarchical human parsing, demonstrating the effectiveness of its hierarchical semantic segmentation framework in achieving high accuracy and robustness in complex visual environments.