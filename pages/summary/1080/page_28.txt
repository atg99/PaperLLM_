The section provides proofs for several propositions related to the sampling lemma and the validity of a gain formula in the context of recurrent neural networks for long sequences. 

In Lemma 3.2, the proof demonstrates how to sample uniformly from a ring defined by two radii, rmin and rmax. It begins by noting the independence of phase and magnitude sampling, with the phase being sampled from a uniform distribution. The proof then derives the cumulative distribution function for the radius and shows that the radius can be parametrized using an exponential function. By applying the inverse sampling theorem, the proof concludes that the expression for ν accurately represents the sampling process.

Proposition 3.3 is validated through numerical simulations, depicted in Figure 10, which compares predicted gains for constant and white noise inputs. The results show that the gain formula holds true for both input types, although it performs better with random inputs. The simulations involved 500 samples and a sequence length of 10,000, with results presented as boxplots indicating median and percentiles. 

Overall, the proofs and numerical validation reinforce the theoretical foundations of the sampling methods and gain predictions for long sequence processing in recurrent neural networks.