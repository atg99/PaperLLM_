The supplementary materials present a simplified implementation of the Linear Recurrent Unit (LRU) using JAX. The LRU's state is updated based on input sequences, following the equation xk = Λxk−1 + exp(γlog)⊙(Buk), where the output is calculated as yk = Cxk + Duk. Key components such as the matrices B and C are initialized using Glorot initialization, with adjustments made to account for output variance. The implementation utilizes an associative scan for efficient parallelization.

The code outlines a forward pass function that computes the output for a given input sequence, initializes the LRU parameters, and defines a binary operator for the parallel scan. The parameters are initialized to ensure the complex values of Lambda are uniformly distributed within specified bounds, and the input/output projection matrices are initialized to facilitate effective learning during model training. The approach aims to enhance the performance of recurrent networks on long sequences by leveraging efficient computations and careful initialization strategies.