This section discusses the selection of optimal hyperparameters for various machine learning models trained on four structural datasets, as summarized in Table 4. The table includes mean loss values from five cross-validations for models such as Gaussian Process Regression (GPR), Support Vector Machine (SVM), Random Forest Regression (RFR), and Artificial Neural Networks (ANN). Each model's hyperparameters and associated loss metrics, including Root Mean Square Error (RMSE) and Mean Absolute Error (MXAE), are detailed.

The selected hyperparameters vary across models, with GPR utilizing a polynomial kernel, while SVM employs different values for parameters C and epsilon. RFR parameters include the number of trees and minimum samples, whereas ANN configurations cover hidden neurons, activation functions, and optimizers.

The text emphasizes the importance of considering training cost and model complexity when selecting hyperparameters for highly accurate machine learning models. Additionally, it discusses the effect of hyperparameter optimization (HOpt) by comparing model performance before and after optimization through boxplots that illustrate the distribution of RMSE and MXAE across the folds of cross-validation. A lower median in these boxplots indicates improved prediction accuracy, reinforcing the efficacy of hyperparameter tuning in enhancing model performance.