The supplemental material provides detailed tables summarizing the results of multi-objective hyperparameter optimization (HOpt) for different models, including Gaussian Process Regression (GPR), Support Vector Machines (SVM), and Random Forest Regression (RFR). 

For GPR, the table lists various configurations with hyperparameters such as kernel types, degrees, scales, and offsets, along with their corresponding Root Mean Square Error (RMSE) and Maximum Absolute Error (MXAE). The average and standard deviation of numerical hyperparameters are also reported, indicating a preference for the polydot kernel with an average RMSE of 0.0571 and MXAE of 0.3630.

In the SVM section, a variety of configurations are presented, highlighting the impact of hyperparameters like C, epsilon, and kernel types on performance metrics. The average values across configurations show an average RMSE of 0.0603 and MXAE of 0.3664, with polydot being the most commonly used kernel.

While the RFR section is mentioned, specific data for this model is not included in the excerpt, indicating that further details may be provided in subsequent sections.

Overall, the tables encapsulate the effectiveness of different hyperparameter settings across the models, emphasizing the significance of kernel choice and numerical hyperparameter tuning in achieving optimal predictive performance.