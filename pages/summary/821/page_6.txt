example, our 1 N pruning results in a top-1 accuracy loss of only 0.591%, which is significantly better than the 5.007% loss experienced by filter pruning. 2) Additionally, our method achieves a notable acceleration on CPU-based devices due to its ability to process operations in parallel and optimize memory usage.

The experiments demonstrate that filter rearrangement enhances model accuracy by preserving larger magnitude weights, as shown in the ablation study. The consecutive kernel removal strategy further contributes to maintaining performance by ensuring that larger magnitude kernels are prioritized during pruning.

Overall, the 1 N pruning method not only maintains superior accuracy compared to traditional pruning methods but also offers significant computational efficiency, making it a viable option for deploying lightweight models in practical applications.