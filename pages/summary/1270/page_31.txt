The document presents performance metrics for various base learners and ensemble methods applied to a Yelp dataset. Key metrics include accuracy and AUC (Area Under the Curve) across different model configurations. 

Table 7 details the base learners, including logistic regression (LogReg) and support vector machine (SVM) models, evaluated with different n-gram representations and feature extraction methods (presence and tf-idf). The accuracy ranges from a minimum of 0.603 for one SVM configuration to a maximum of 0.941 for another. AUC values also vary, with the highest recorded at 0.987.

Table 8 summarizes the results of ensemble learners, combining multiple base learners. The configurations are categorized by discrete and score-based methods, employing simple, weighted, and meta approaches. The best ensemble accuracy is 0.924 with a meta approach, while the maximum AUC reaches 0.988.

Overall, the results indicate that ensemble methods generally outperform individual base learners, highlighting the effectiveness of combining models to enhance predictive performance on the Yelp dataset.