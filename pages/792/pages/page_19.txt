Compression of Deep Learning Models for Text: A Survey TKDD, Aug 2020, TKDD
3.2.3 Hybrid Ternary Quantization. Given various ternary quantization methods proposed so far, one can combine
them and use different methods for different layers. Wang et al. [ 127] found that threshold ternary quantization (TTQ)
(Eq. 32) is preferable for weights in an RNN while Bernoulli Ternary Quantization (BTQ) is preferable for activations.
This is based on the observation that in an RNN, the distribution of weights follows normal distribution (with different
ranges across different weight matrices), while for activations, the range is [0,1] and most of the values are located near
to the two poles instead of the middle of the range. In the training phase (where we need to store the full precision
weights), ternary quantization of weights only saves 1.4x memory consumption but quantizing both weights and
activations can achieve up to 16x memory savings.
The HitNet architecture [ 127] with this hybrid ternary quantization can be defined using these equations, where
𝑖𝑡,𝑓𝑡,𝑜𝑡are the input, forget and output gates; 𝑥𝑡is input at time 𝑡;𝑐𝑡is the cell output; and ℎ𝑡is the hidden layer
output;𝑊𝑥,𝑊ℎ,𝑏𝑥,𝑏ℎare weights.
𝑖𝑡,𝑓𝑡,𝑔𝑡,𝑜𝑡=𝜎(TTQ(𝑊𝑥)𝑥𝑡+TTQ(𝑏𝑥)
+TTQ(𝑊ℎ)ℎ𝑡−1+TTQ(𝑏ℎ))
𝑐𝑡=𝑓𝑡×𝑐𝑡−1+𝑖𝑡×𝑔𝑡
ℎ𝑡=BTQ(𝑜𝑡×𝜎(𝑐𝑡)) (35)
3.3 General Quantized Networks
So far we discussed methods designed specifically for binary and ternary quantization. Now, we discuss general
𝑘-bit quantization methods. We will discuss (1) uniform quantization methods which perform equal width binning,
(2) non-uniform methods which are closer to equal frequency binning, (3) loss-aware quantization methods, and (4)
methods specifically designed for Transformer models.
3.3.1 Uniform Quantization. Uniform𝑘-bit Quantization simply splits the range of original weights into 2𝑘−1equal
size intervals [40, 47, 97]. Refer Fig. 3(C). If original weights are in range [-1,1], they can be quantized as follows.
𝑞𝑘(𝑥)=2©­­
«round[(2𝑘−1)
𝑥+1
2
]
2𝑘−1−1
2ª®®
¬(36)
Similarly, if entries are in range [0,1], we could use the following formula.
𝑞𝑘(𝑥)=1
2𝑘−1⌊(2𝑘−1)𝑥+1
2⌋ (37)
When the weights in matrix 𝑋are not in the range [0,1], we can first scale weights as follows.
˜𝑋=𝑋−𝛽
𝛼(38)
where𝛼=max(𝑋)−min(𝑋)and𝛽=min(𝑋). After quantization, we can apply a reverse transform to approximate
the original values. Overall, the quantized result can be written as follows.
𝑄(𝑋)=𝛼𝑞𝑘(˜𝑋)+𝛽 (39)
19