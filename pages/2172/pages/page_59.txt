Moreover, defence techniques are closely related to the verication techniques.
Every verication method can serve as a defence method, for its ability to
identify the adversarial examples with guarantees. For an adversarial input, it
may be classied wrongly if directly passing the DNN. To defence this, a DNN
verier can step in and determine if it is an adversarial example. If it is, the
verication output can be utilised to alert that the classication from the DNN
is not trustable.
58