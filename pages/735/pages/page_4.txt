1470 Machine Learning (2019) 108:1467–1501
2.1 Reinforcement learning and policy search
We consider RL in an environment governed by a Markov Decision Process (MDP). An
MDP is described by the tuple /angbracketleftS,A,P,R,μ1/angbracketright,w h e r e S⊆Rdsis the state space, A⊆Rda
is the action space, P/parenleftbig
s/prime|s,a/parenrightbig
deﬁnes a Markovian transition probability density between
the current sand the next state s/primeunder action a,R(s,a)is the reward function, and μ1is
initial distribution for state s1. Given such an environment, the goal of a RL is to learn to act.
F o r m a l l y ,w ew a n tt oﬁ n da policy π(a|s)to take an appropriate action when the environment
is in state s. By following such a policy starting at initial state s1, we obtain a sequence of
states, actions and rewards (st,at,rt)t=1...T,w h e r e rt=R(st,at)is the reward at time t(T
is the total timesteps). We refer to such sequences as trajectories orepisodes . Our goal is to
ﬁnd a policy that maximizes the expected return of such trajectories,
maxπEμπ(s)π(a|s)/bracketleftbig
Qπ(s,a)/bracketrightbig
, (1)
where Qπ(st,at):= E/producttextT
i=t+1π(ai|si)P(si+1|si,ai)/bracketleftBiggT/summationdisplay
i=tγi−tri+1/bracketrightBigg
, (2)
where μπ(s)is the state distribution under π, i.e., the probability of visiting state sunder
π,Qπ(s,a)is the action-state value function (or Q-function) which is the expected return
obtained by executing ain state sand then following π, and, ﬁnally, γ∈[0,1)is the discount
factor which assigns weights to rewards at different timesteps.
One way to solve the optimization problem of Eq. ( 2) is to use policy search (Deisenroth
et al. 2013 ), e.g., we can use a parameterized policy function π(a|s;θ)with the parameter θ
and take the following gradients steps
θi+1=θi+αθ∇θEμπ(s)π(a|s;θ)/bracketleftbig
Qπ(s,a)/bracketrightbig/vextendsingle/vextendsingle
θ=θi, (3)
where αθ>0 is the stepsize and iis a learning iteration. There are many ways to compute a
stochastic estimate of the above gradient, e.g., we can ﬁrst collect one trajectory starting froms
1∼μ1(s), compute Monte Carlo estimates /hatwideQπ(st,at)ofQπ(st,at), and then compute the
gradient using REINFORCE (Williams 1992 )a s
∇θEμπ(s)π(a|s;θ)/bracketleftbig
Qπ(s,a)/bracketrightbig
≈T/summationdisplay
t=1/bracketleftbig
∇θlogπ(at|st;θ)/bracketrightbig/hatwideQπ(st,at),
where /hatwideQπ(st,at)=R(st,at)+γ/hatwideQπ(st+1,at+1),∀t=1,2,..., T−1,(4)
and/hatwideQπ(sT,aT):= R(sT,aT)=rT. The recursive update to estimate /hatwideQπis due to the
deﬁnition of Qπshown in Eq. ( 2). The above stochastic gradient algorithm is guaranteed
to converge to a locally optimal policy when the stepsize are chosen according to Robbins-Monro conditions (Robbins and Monro 1985 ). However, in practice, using one trajectory
might have high variance and the method requires averaging over many trajectories whichcould be inefﬁcient. Many solutions have been proposed to solve this problem, e.g., baselinesubstraction methods (Greensmith et al. 2004 ;G ue ta l . 2016a ;W ue ta l . 2018 ). Actor-critic
methods are one of the most popular methods for this purpose.
2.2 Actor-critic methods and their instability
In actor-critic methods, the value function is estimated using a parameterized function approx-imator, i.e., Q
π(s,a)≈/hatwideQ(s,a;ω),w h e r e ωare the parameters of the approximator such
123