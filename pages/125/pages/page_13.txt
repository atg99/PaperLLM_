INTRODUCTION TO AUTOENCODERS
Figure 4: In the top line, you can see the original digits from the MNIST
dataset. In contrast, the line below contains the digits reconstructed by the
autoencoder with number of neurons equal to (784, 16, 784).
tained in a much lower number of features than 784.
NoteAn autoencoder with a middle layer smaller than the input di-
mensions (a bottleneck) can be used to extract the essential features of
an input dataset creating a learned representation of the inputs given
by the function g(xi). Eﬀectively an FFA can be used to perform di-
mensionality reduction.
TheFFAwillnotrecreatetheinputdigitswellifthenumberofneuronsinthe
middle layer is reduced too much (if the bottleneck is too extreme). Figure
6 shows the reconstruction of the same digits with an autoencoder with only
8 neurons in the middle layer. With only 8 neurons in the middle layer, you
can see that some reconstructed digits are wrong. As you can see in Figure
6 the 4 is reconstructed as a 9 and a 2 is reconstructed to something that
resembles a 3. In Figure 7, you can compare the reconstructed digits by all
Figure 6: In the top line you can see the original digits from the MNIST
dataset. In contrast, the line below contains the digits reconstructed by the
autoencoder with number of neurons equal to (784, 8, 784).
13