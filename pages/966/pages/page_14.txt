The value function underlying an MRP is dened the same way and is denoted by V:
V(x) =E"1X
t=0
tRt+1X0=x#
; x2X:
It will also be useful to dene the action-value function ,Q:XA! R, underlying a
policy2statin an MDP: Assume that the rst action A0is selected randomly such that
P(A0=a)>0 holds for all a2A, while for the subsequent stages of the decision process
the actions are chosen by following policy . Let ((Xt;At;Rt+1);t0) be the resulting
stochastic process, where X0is as in the denition of V. Then
Q(x;a) =E"1X
t=0
tRt+1X0=x;A 0=a#
; x2X;a2A:
Similarly to V(x), the optimal action-value Q(x;a) at the state-action pair ( x;a) is dened
as the maximum of the expected return under the constraints that the process starts at state
x, and the rst action chosen is a. The underlying function Q:XA! Ris called the
optimal action-value function .
The optimal value- and action-value functions are connected by the following equations:
V(x) = sup
a2AQ(x;a); x 2X;
Q(x;a) =r(x;a) +
X
y2XP(x;a;y )V(y); x2X;a2A:
In the class of MDPs considered here, an optimal stationary policy always exists:
V(x) = sup
2statV(x); x2X:
In fact, any policy 2statwhich satises the equality
X
a2A(ajx)Q(x;a) =V(x) (8)
simultaneously for all states x2X is optimal. Notice that in order (8) to hold, (jx)
must be concentrated on the set of actions that maximize Q(x;). In general, given some
action-value function, Q:XA! R, an action that maximizes Q(x;) for some state xis
called greedy with respect to Qin statex. A policy that chooses greedy actions only with
respect toQinallstates is called greedy w.r.t. Q.
Thus, a greedy policy with respect to Qis optimal, i.e., the knowledge of Qalone is
sucient for nding an optimal policy. Similarly, knowing V,randPalso suces to act
14