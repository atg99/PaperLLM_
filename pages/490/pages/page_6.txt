Restricted Boltzmann Machine and Deep Belief Network: Tutorial and Survey 6
joint distribution is the product of every distribution. We
can write similar expressions for the probability P(vjh):
P(vjh) =1
Z00exp(b>v+v>Wh)
=1
Z00expdX
i=1bivi+dX
i=1viWi:h
=1
Z00dY
i=1exp(bivi+viWi:h); (16)
where Wi:2Rpdenotes the i-th row of matrix Wand
Z00:=P
v2Rdexp(b>v) exp(v>Wh). This equation
shows that given the hidden variables, the visible variables
are conditionally independent. Q.E.D.
According to Eq. (15) and considering the rule P(hjv) =
P(h;v)=P(v), we have:
P(hjv) =1
Z0pY
j=1exp(cjhj+v>W:jhj)
=1
Z0pY
j=1P(hj;v)
=)P(hj;v) = exp(cjhj+v>W:jhj)
= exp(cjhj+dX
i=1viwijhj):(17)
Similarly, according to Eq. (16) and considering the rule
P(vjh) =P(h;v)=P(h), we have:
P(hjv) =1
Z00dY
i=1exp(bivi+viWi:h)
=1
Z00dY
i=1P(h;vi)
=)P(h;vi) = exp(bivi+viWi:h)
= exp(bivi+pX
j=1viwijhj):(18)
We will use these equations later.
3.3. Sampling Hidden and Visible Variables
3.3.1. G IBBS SAMPLING
We can use Gibbs sampling for sampling and generating
the hidden and visible units. If denotes the iteration index
of Gibbs sampling, we iteratively sample:
h()P(hjv()); (19)
v(+1)P(vjh()); (20)1Input : visible dataset v, (initialization: optional)
2Get initialization or do random initialization of v
3while until burn-in do
4 forjfrom 1topdo
5h()
jP(hjjv())
6 forifrom 1toddo
7v(+1)
iP(vijh())
Algorithm 1: Gibbs sampling in RBM
until the burn-in convergence. As was explained in Section
2.2, only several iterations of Gibbs sampling are usually
sufﬁcient. After the burn-in, the samples are approximate
samples from the joint distribution P(v;h). As the vari-
ables are conditionally independent, this Gibbs sampling
can be implemented as in Algorithm 1. In this algorithm,
h()
jP(hjjv())can be implemented as drawing a sam-
ple from uniform distribution uU[0;1]and compar-
ing it to the value of Probability Density Function (PDF),
P(hjjv()). Ifuis less than or equal to this value, we have
hj= 1; otherwise, we have hj= 0. Implementation of
samplingvihas a similar procedure. Alternatively, we can
use inverse of cumulative distribution function of these dis-
tributions for drawing samples (see (Ghojogh et al., 2020)
for more details about sampling).
3.3.2. G ENERATIONS AND EVALUATIONS BY GIBBS
SAMPLING
Gibbs sampling for generating both observation and hid-
den units is used for both training and evaluation phases
of RBM. Use of Gibbs sampling in training RBM will be
explained in Sections 3.4 and 3.5. After the RBM model
is trained, we can generate any number of p-dimensional
hidden variables as a meaningful representation of the d-
dimensional observation using Gibbs sampling. More-
over, using Gibbs sampling, we can generate other d-
dimensional observations in addition to the original dataset.
These new generated observations are d-dimensional rep-
resentations for the p-dimensional hidden variables. This
shows that BM and RBM are generative models.
3.4. Training Restricted Boltzmann Machine by
Maximum Likelihood Estimation
The weights of links which are W,b, andcshould be
learned so that we can use them for sampling/generating
the hidden and visible units. Consider a dataset of nvisible
vectorsfvi2Rdgn
i=1. Note that vishould not be confused
withviwhere the former is the i-th visible data instance and
the latter is the i-th visible unit. We denote the j-th dimen-
sion of vibyvi;j; in other words, vi= [vi;1;:::;vi;d]>.