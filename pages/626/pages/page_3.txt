Characterizing and overcoming the greedy nature of learning in multi-modal deep neural networks
Specifically, we implement each fusion module with a multi-
modal transfer module (MMTM) (Joze et al., 2020). It
connects corresponding convolutional layers from different
uni-modal branches. Let A0∈RN1×···× NL×CandA1∈
RM1×···× MJ×C′denote feature maps produced by these two
layers. Here, NiandMirepresent the spatial dimensions
of each feature map, and CandC′represent the number of
feature maps. We apply global average pooling over spatial
dimensions of the feature maps and get two vectors, denoted
byh0∈RCandh1∈RC′. A fusion mechanism within
MMTM is then applied to the two vectors:
w0, w1=g([h0, h1]) (1)
where [·,·]represents the concatenation operation; w0∈RC
andw1∈RC′. The function gis parameterised with a
fully-connected layer, ReLu (Nair & Hinton, 2010) function,
and two more fully-connected layers, each returning an
activation in the dimension of CandC′.
Next, we re-scale the feature maps ( A0andA1) with the
obtained activations ( w0andw1):
˜A0= 2×σ(w0)⊙A0,˜A1= 2×σ(w1)⊙A1(2)
where ⊙is the channel-wise product operation and σ(·)is
the sigmoid function.
We feed ˜A0and ˜A1to the next layer of ϕ0andϕ1. Thus in-
formation from one modality is shared from one uni-modal
branch to the other.
We train a multi-modal DNN fonDtrain. Let ˆybe the
prediction of ffor an input x= (xm0,xm1):
ˆy=f(xm0,xm1). (3)
As shown in Figure 1, ˆy=1
2(ˆy0+ ˆy1), where ˆy0andˆy1are
the outputs of the two uni-modal branches.
During training, the parameters of fare updated by stochas-
tic gradient descent (SGD) to minimize the loss:
L= CE( y,ˆy0) + CE( y,ˆy1) (4)
where CEstands for cross-entropy. We refer to each of the
cross-entropy losses as a modality-specific loss. We train
the model until it reaches 100% classification accuracy on
Dtrain, and pick the model checkpoint associated with the
highest validation accuracy across all epochs.
4. The Greedy Learner Hypothesis
In this section, we first derive conditional utilization rate
to help explain the multi-modal DNN and to quantify its
imbalance in modality utilization. Then we introduce the
greedy learner hypothesis to explain challenges observed in
training multi-modal DNNs.
xm0xm1
/uni0302y0/uni0302y1
/uni0302yFigure 1. The multi-modal DNN with intermediate fusion that we
study in this work. Blue and red blocks represent the two uni-
modal networks, ϕ0andϕ1, learning from modalities m0andm1.
The fusion layers are marked with green, green-blue, and green-
red. We denote the predictions from ϕ0andϕ1byˆy0andˆy1, and
the average of the two by ˆywhich is the model’s prediction.
4.1. Conditional Utilization Rate
In the multi-modal DNN shown in Figure 1, each uni-modal
branch largely focuses on the associated input modality,
and the fusion modules generate context information using
all modalities, and feed the information to the uni-modal
branches. Both ˆy0andˆy1depend on information from both
modalities. We derive f0andf1from f:
ˆy0=f0(xm0,xm1),ˆy1=f1(xm0,xm1). (5)
LetDtrain={xi}n
i=1andh0(xi)andh1(xi)denote the
value of h0andh1when the model takes xias input. We
compute the average of h0andh1overDtrain:
h0=1
nnX
i=1h0(xi),h1=1
nnX
i=1h1(xi). (6)
To estimate f’s reliance on each modality independently,
we modify the operations introduced in §3 for each fusion
module as below:
w0,∗=g([h0,h1]),∗, w1=g([h0, h1]). (7)
With such modified operation in every fusion module in the
multi-modal DNN, we cut off the road to share information
between the uni-modal branches and let the output of each
branch rely on a single modality. We derive f′
0andf′
1and
denote their outputs by ˆy′
0andˆy′
1:
ˆy′
0=f′
0(xm0),ˆy′
1=f′
1(xm1). (8)
In summary, we derive four models from fand compute
the accuracy of each on Dtest, denoted by A(·). We group
the four accuracies into two pairs: (A(f0), A(f′
0)), and
(A(f1), A(f′
1)).